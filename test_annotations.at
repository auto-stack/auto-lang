type TestType {
    [vm]
    static fn new() TestType

    [vm]
    fn do_something(x int) void

    [c, vm]
    fn hybrid_method(s str) void

    static fn regular_static() int

    fn regular_method() void
}

fn main() {
    "success"
}
