// Hand-written TS util consumed by the widget `use { fn: ... }` block.
// Copied into the generated Vue project as src/ext/src/front/utils/greet.ts
// and imported from the SFC as `@/ext/src/front/utils/greet`.

export function greet(name: string): string {
  return `hello, ${name}!`
}

export function farewell(name: string): string {
  return `bye, ${name}!`
}
