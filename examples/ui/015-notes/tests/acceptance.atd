# 015-notes 验收契约

> 本文件是 015-notes 功能测试的 **single source of truth**。
> 每个 T 条目声明一个验收场景（领域语言），对应的 Playwright 测试在
> `smoke.spec.ts` / `accent-dark.spec.ts` 里用同一 ID 实现，
> MCP 可执行子集在 `015-notes.autotest`（`run_autotest.py` 驱动）。
>
> **契约依据**引用 `docs/plans/363-autoui-generation-skill.md` 的
> generator-contracts.md（C-XXX 编号）。
>
> **PLAN-616 重写说明**：本契约随「清爽化重做」整体重写。重点变化——
> ① 去掉模态编辑（Edit/Save/Cancel 模式）改为**始终可编辑 + 显式保存**；
> ② 搜索由 known-gap 改为**强断言**；③ 标签筛选由死代码改为**强断言**；
> ④ 置顶要求**落库**；⑤ 删除要求**两步确认**；⑥ 视图筛选改为
> All/Pinned/文件夹/标签 胶囊（旧 All/Pinned/Recent 分段控件退役）。

---

## 导航类

### T1: 笔记切换
- **操作**：点击列表中的笔记行（如 `Shopping List`）
- **期望**：编辑区标题与正文切换为该笔记内容
- **契约**：C1（同名组件 v-if 分支唯一 key）、C6（for 循环 key）
- **实现**：`NavTree.NoteRow.onclick → store.SelectNote(k)`（k 是索引表元素 = `.notes` 下标）

### T2: 视图筛选（替换旧 View tabs）
- **操作**：点击筛选胶囊 `All` / `Pinned` / 文件夹名
- **期望**：`Pinned` 只列置顶笔记（普通列表为空）；`All` 恢复全量且**置顶笔记进
  `Pinned` 分组**、其余进 `Notes` 分组；文件夹胶囊只列该文件夹下的笔记
- **实现**：`store.ApplyFilter` 产出 `visible_pinned` / `visible_notes` 索引表

### T2-GROUP: 分组与选中
- **期望**：选中行使用侧栏契约的 active 底色；`Notes` 分组标签常驻可见
- **历史**：旧契约用 `📁` emoji 断分组标题（Plan 562 nav→sidebar 迁移后 emoji 消失，
  断言随之失真）——本契约改为断言**分组标签文本**，不再依赖 emoji

---

## 筛选类

### T3: 搜索（原 known-gap，现为强断言）
- **操作**：在搜索框输入 `milk` → 列表只剩正文命中的 `Shopping List`（**大小写不敏感**），无 `Welcome`
- **操作**：清空搜索 → 恢复全量列表
- **实现**：`store.SetSearch(q) → ReloadNotes()`，命中集由后端 `search_notes`
  （内部 `to_lower`）给出，前端不做逐条比对

### T3b: 搜索无命中空态
- **操作**：输入 `zzzzz`
- **期望**：出现 `No matching notes` 空态（与「一条笔记都没有」的空态区分）

### T4: Tag 筛选（原死代码，现为强断言）
- **操作**：点击标签胶囊 `#home`
- **期望**：`active_tag = "home"`，列表只剩带该标签的笔记；点 `All` 清除
- **实现**：`all_tags` 由 `computed`（恒空）改为**模型字段**，由 `.SeedVocab` 从笔记数据派生

---

## 编辑类

### T5: 始终可编辑 + 显式保存
- **操作**：直接编辑标题/正文 → 进入未保存态（`dirty = true`，出现 `Save` 与 `Unsaved changes`）
- **操作**：点击 `Save`（或标题回车）→ 落库并回到已保存态（`dirty = false`，状态回到 `Saved`），列表行标题同步更新
- **契约**：C4（view 引用的 handler 必须在 on 块定义）
- **历史**：旧契约是「Edit → Save/Cancel 模态循环」，`Cancel` 丢弃改动；新模型无读/写模式切换

### T5c: 切换笔记自动落盘（回归）
- **操作**：编辑标题但不保存 → 点击另一条笔记 → 再切回来
- **期望**：改动仍在（前一条草稿在切换时已自动落库）
- **历史教训**：旧模态模型下「编辑中切换笔记」会丢草稿，这是本次重做的核心动机之一

### T6: 创建新笔记
- **操作**：点击 `New note`
- **期望**：后端新增一条空笔记，并**自动选中它**（编辑区标题为空、可直接打字）
- **历史**：旧契约把「新建不自动选中」记为「已接受瑕疵」——本计划修掉

### T7: 删除需两步确认
- **操作**：点击 `Delete` → 出现确认（`Delete` / `Cancel`）；点 `Cancel` 不删除
- **操作**：再次点击 `Delete` 确认 → 笔记被删除，列表少一行
- **历史**：旧契约是一击即删（无撤销）

---

## 元数据类

### T8: Pin 置顶（落库）
- **操作**：点击 `Pin note` → 按钮变为 `Unpin`；`Pinned` 分组出现该笔记
- **操作**：切换筛选并重新选中该笔记 → 仍显示 `Unpin`（**已落库**，非仅内存翻转）
- **历史**：旧实现 `TogglePin(idx)` 只翻转本地 `notes[idx].pinned`，刷新即丢

### T9: Tag 增删
- **操作**：点 `Tag` → 输入标签名 → 回车（或点确认）→ 标签出现在笔记胶囊与筛选胶囊两处并持久化
- **操作**：点标签胶囊上的 `×` → 标签被移除
- **已知限制**：筛选胶囊词表不回收「已删除标签」（清空重建受 VM codegen 限制，见 PLAN-616 §10.3）

### T10: 文件夹作用域下新建
- **操作**：先选某个文件夹胶囊，再点 `New note`
- **期望**：新笔记创建在该文件夹下（清空搜索以保证可见）
- **历史**：旧契约是「文件夹标题旁的 `+` 按钮」；新 UI 无分组级按钮，改为跟随当前作用域

---

## 主题类

### T11: Dark / Light 切换
- **操作**：`Settings` → `Light`；再 `Dark`
- **期望**：`dark_mode` 状态与根元素 `dark` class 同步翻转
- **注意**：顶栏图标按钮只做快切；`Settings` 面板的 `Dark`/`Light` 是显式设定（`SetMode`），避免「点 Dark 反而切到 Light」

### T12: 5 色主题切换（亮色模式）
- **操作**：依次点击外观面板 5 个色板
- **期望**：`<html>` 的 `--primary` 变为对应 HSL；`New note` 按钮底色跟随；
  `localStorage['notes-accent-color']` 记录选择
- **契约**：C-ACCENT-1

### T12-DARK: 5 色主题切换（暗色模式）⚠️ 关键回归测试
- **期望**：暗色下 5 色板仍驱动 `New note` 底色（**不回落为近白**）；暗色亮度比亮色高约 +10%（Plan 601 统一值）
- **契约**：**C-DARK-1**——`.dark` class 元素与 inline `--primary` 写入必须同元素
- **历史教训**：Plan 360 曾把 `--primary` 写到 `<html>` 而 `.dark` 在 `#app > div` 上

### T13: 控制台无实质错误
- **允许**：favicon 404、第三方非致命警告
- **禁止**：`view is not available`、`Unhandled error`、`store is not defined`

---

## 生成器契约依据汇总

| 契约 ID | 内容 | 关联测试 |
|---------|------|----------|
| C1 | 同名组件在 v-if 分支必须获得唯一稳定 key | T1 |
| C6 | for 循环列表项自动绑定 key | T1 |
| C-ACCENT-1 | accent 写入的 CSS 变量必须覆盖 Tailwind `--primary` 默认 | T12, T12-DARK |
| **C-DARK-1** | `.dark` class 与 `--primary` inline 修改必须同元素 | **T12-DARK** |
| C4 | view 引用的 handler 必须在 on 块定义 | T5, T8, T9 |

---

## 桌面模式行为契约（Plan 370 + Plan 371）

以下 D 条目声明跨平台行为约束。桌面测试由 `desktop_behavior.rs`（Headless）、
`desktop_mcp.py`（MCP）、`015-notes.autotest`（MCP 场景声明）执行。

### D1: 状态初始化 — Web ✅ / VM ✅ / Rust ✅
### D2: Handler 执行 — Web ✅ / VM ✅ / Rust ✅
### D3: 事件参数传递 — Web ✅ / VM ⚠️（历史字符串赋值溢出） / Rust ✅
### D4: 布尔状态切换 — Web ✅ / VM ✅ / Rust ✅
### D5: 条件逻辑 — Web ✅ / VM ✅ / Rust ✅
### D6: 视图模板完整性 — Web ✅ / VM ✅ / Rust ✅
### D7: MCP 交互闭环 — Web ✅（Playwright）/ VM ✅（MCP）/ Rust ✅（MCP）
- 工具：`autoui_snapshot, autoui_find, autoui_exists, autoui_action,
  autoui_state, autoui_inspect, autoui_keyboard, autoui_type, autoui_screenshot`
- Rust 限制：`autoui_state` 不可用 → `skip_if rust`

---

## 桌面模式 known-gap（Plan 370）

- D-GAP-1 语义 CSS token — ✅ 已修复
- D-GAP-2 dark mode — ✅ 已修复
- D-GAP-3 AutoDownEditor（Tiptap 富文本）— ⚠️ 部分修复；**PLAN-616 决定**正文继续用原生
  `textarea`（纯文本），不引入富文本编辑器
- D-GAP-4 store composable — ✅ 已修复
- D-GAP-5 accent 主题色 — ✅ 已修复

---

## PLAN-616 新增：示例侧 DSL/VM 使用约束

本次重做在真实后端数据上实证了一批「VM 端不可用/会 panic」的写法，
作为**示例可依赖的 DSL 子集**记录（完整清单与探针方法见 PLAN-616 §4.2）：

| 约束 | 现象 | 替代写法 |
|---|---|---|
| `text "…${x}…"` 插值 | 两端都渲染成字面量 | `text <ref>` / `text <prop.field>` |
| `view fn` 标量 prop 做文本 | VM 端文本节点不渲染 | 只传对象 prop + 点路径（如 `note: Note`） |
| `.field = []` | VM codegen panic `Assignment to complex LHS` | 用类型化局部量构建后整体赋值 |
| 局部 `[]str`/`[]Note` → 状态字段 | 同上 panic | 局部 `[]int` 可赋值；或直接 `push` 到状态字段 |
| 视图条件里的方法调用 `x.contains(y)` | VM 端恒假（`resolve_binding_path` 不支持方法调用） | 过滤下沉到 store 产出**索引表**，视图只解引用 |
| `flex-wrap` | VM 不支持（降级单行） | 拆成多行容器 |
| 按钮无显式宽度 | VM 端按 Fill 撑开布局 | 按钮加 `w-auto` |
| 无文字标签的纯图标按钮 | MCP `autoui_find` 按 label 找不到 | 需要被场景驱动的控件带文字标签 |

---

## 维护约定

- 改 .at 功能时，**同步更新本契约**（加/改/删 T 条目）
- 新增生成器契约时，在"契约依据汇总"加行，并关联到对应 T
- 修复 bug 时，加一个**回归测试**条目（如 T12-DARK），标注"历史教训"
- 新增桌面特性或修复 D-GAP 时，**同步更新 D/D-GAP 条目**
- 新增 DSL/VM 使用约束时，补进「示例侧 DSL/VM 使用约束」表
