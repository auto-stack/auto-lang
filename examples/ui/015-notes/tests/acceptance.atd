# 015-notes 验收契约

> 本文件是 015-notes 功能测试的 **single source of truth**。
> 每个 T 条目声明一个验收场景（领域语言），对应的 Playwright 测试在
> `smoke.spec.ts` / `accent-dark.spec.ts` 里用同一 ID 实现。
>
> **契约依据**引用 `docs/plans/363-autoui-generation-skill.md` 的
> generator-contracts.md（C-XXX 编号）。

---

## 导航类

### T1: 笔记切换
- **当** 用户点击侧栏中的 "Shopping List"
- **那么** 编辑区应显示包含 "Milk" 的内容
- **且** 不应在控制台出现 Tiptap unmount 错误
- **契约依据**：C1（组件实例身份）、C6（列表渲染与 key）

### T2: View tabs 切换
- **当** 用户点击 "Pinned" tab
- **那么** 列表只显示 `pinned == true` 的笔记
- **当** 用户点击 "Recent" tab
- **那么** 列表显示全部笔记（无文件夹分组）
- **当** 用户点击 "All" tab
- **那么** 列表按 Notes / Work / Personal 文件夹分组

---

## 筛选类

### T3: 搜索（当前未实现，标记为 known-gap）
- **状态**：⚠️ **功能缺失**。搜索框能输入但不过滤列表。
- **预期**（修复后）：用户输入 "Shopping"，列表只剩标题/正文匹配的笔记。
- **跟踪**：记录在 015-notes 待办，本契约暂不强制执行。

### T4: Tag 筛选
- **当** 用户点击侧栏的 "work" tag 筛选按钮
- **那么** 列表只显示 `tags` 包含 "work" 的笔记
- **当** 再次点击同一 tag
- **那么** 清除筛选，恢复完整列表

---

## 编辑类

### T5: Edit / Save / Cancel 循环
- **当** 用户点击 "Edit"
- **那么** 编辑器应显示当前笔记内容（非空）
- **且** 标题变为可编辑输入框，预填当前标题
- **当** 用户修改标题后点击 "Save"
- **那么** 新标题持久化到后端（刷新后仍存在）
- **当** 用户点击 "Cancel"（未保存）
- **那么** 返回只读模式，内容保持原样
- **契约依据**：C1（双 editor v-if 切换必须正确）

### T6: 创建新笔记
- **当** 用户点击 header 的 "New" 按钮
- **那么** 后端应创建一个空笔记
- **且** 该笔记出现在列表中
- **状态**：⚠️ 新笔记不自动选中/进入编辑模式（体验瑕疵）

### T7: 删除笔记
- **当** 用户在编辑区点击 "Delete"
- **那么** 当前笔记从后端删除
- **且** 列表更新，编辑区切换到剩余的第一个笔记（如有）

---

## 元数据类

### T8: Pin 置顶切换
- **当** 用户在编辑区点击 📌 按钮
- **那么** 该笔记的 `pinned` 状态切换
- **且** 在 "Pinned" tab 中相应地出现/消失

### T9: Tag 添加
- **当** 用户 hover tag 行并点击 "+ tag"
- **那么** 出现 tag 名输入框
- **当** 输入 "testtag" 并点 "✓"
- **那么** 该 tag 出现在笔记上 + 侧栏筛选区
- **且** 持久化到后端

### T10: 文件夹内新建
- **当** 用户在 "Work" 文件夹标题旁点击 "+"
- **那么** 创建一个 `folder == "work"` 的新笔记
- **状态**：功能存在但未深度验证（依赖 T6 的体验修复）

---

## 主题类

### T11: Dark mode 切换
- **当** 用户点击 "🌙 Dark" 按钮
- **那么** 应用根元素获得 `dark` class
- **且** body 背景平滑过渡（`transition: background-color`）
- **当** 再次点击（显示为 "☀ Light"）
- **那么** 移除 `dark` class，回到亮色

### T12: 5 色主题切换（亮色模式）
- **当** 用户依次点击 indigo / coral / ocean / sage / amber 色板
- **那么** `<html>` 的 `--primary` CSS 变量依次变为对应 HSL
- **且** "New" 按钮的背景色实时跟随
- **且** `localStorage['notes-accent-color']` 保存当前选择
- **契约依据**：C-ACCENT-1（accent 写入的 CSS 变量必须覆盖 Tailwind 默认）

### T12-DARK: 5 色主题切换（暗色模式）⚠️ 关键回归测试
- **当** 用户切换到 Dark 模式
- **且** 依次点击 5 个色板
- **那么** "New" 按钮的背景色必须跟随（不是暗色模式的默认近白色）
- **且** 暗色模式下 lightness 应比亮色高 ~4%（对比度补偿）
- **契约依据**：**C-DARK-1**（`.dark` class 所在元素与 `--primary` inline 修改必须在同一 DOM 元素，否则子元素的 `.dark { --primary }` 规则会覆盖 `<html>` 的继承值）
- **历史教训**：Plan 360 实施时，applyAccent 把 `--primary` 写到 `<html>`，但 `.dark` class 在 `#app > div` 上，导致暗色模式下 accent 失效。本测试正是为防此类跨层冲突回归而设。

### T13: 控制台无实质错误
- **当** 执行上述所有操作时
- **那么** 浏览器控制台不应出现 JavaScript 错误
- **允许**：favicon 404、第三方库的非致命警告
- **禁止**：Tiptap "view is not available"、Vue "Unhandled error"、`store is not defined`

---

## 生成器契约依据汇总

本契约引用的生成器契约（详见 Plan 363 的 generator-contracts.md）：

| 契约 ID | 内容 | 关联测试 |
|---------|------|----------|
| C1 | 同名组件在 v-if 分支必须获得唯一稳定 key | T1, T5 |
| C6 | for 循环列表项自动绑定 key | T1 |
| C-ACCENT-1 | accent 写入的 CSS 变量必须覆盖 Tailwind `--primary` 默认 | T12, T12-DARK |
| **C-DARK-1** | `.dark` class 与 `--primary` inline 修改必须同元素 | **T12-DARK** |
| C4 | view 引用的 handler 必须在 on 块定义 | T5, T8, T9 |

---

## 桌面模式行为契约（Plan 370）

以下 D 条目声明跨平台行为约束。标注了每个平台的支持状态。
桌面测试由 `desktop_behavior.rs`（Headless）和 `desktop_mcp.py`（MCP）执行。

### D1: 状态初始化
- **当** widget 挂载并触发 Init
- **那么** model 声明的状态字段应全部可读
- **且** 初始值与 .at 声明一致
- **平台**: Web ✅ | VM ✅ | Rust ✅
- **测试**: desktop_behavior.rs D1

### D2: Handler 执行
- **当** 触发 .Inc handler
- **那么** count 状态正确递增
- **平台**: Web ✅ | VM ✅ | Rust ✅
- **测试**: desktop_behavior.rs D2, desktop_mcp.py T-MCP-3

### D3: 事件参数传递
- **当** handler 接收字符串参数（如 SetName(n)）
- **那么** 参数值正确传递到 handler body
- **平台**: Web ✅ | VM ⚠️（VM string assignment 有 overflow bug） | Rust ✅
- **测试**: desktop_behavior.rs D3

### D4: 布尔状态切换
- **当** 触发 .Toggle handler（dark_mode = !dark_mode）
- **那么** 布尔状态正确翻转
- **平台**: Web ✅ | VM ✅ | Rust ✅
- **测试**: desktop_behavior.rs D4

### D5: 条件逻辑
- **当** handler body 含 if 条件
- **那么** 条件分支正确执行
- **平台**: Web ✅ | VM ✅ | Rust ✅
- **测试**: desktop_behavior.rs D5

### D6: 视图模板完整性
- **当** widget 构造完成
- **那么** view_template 生成有效的 AuraNode 树
- **平台**: Web ✅ | VM ✅ | Rust ✅
- **测试**: desktop_behavior.rs D6, desktop_mcp.py T-MCP-1

### D7: MCP 交互闭环
- **当** 通过 MCP 工具 snapshot → click → state 查询
- **那么** UI 快照正确、点击触发 handler、state 实时更新
- **平台**: Web ✅（Playwright） | VM ✅（MCP 9247） | Rust ❌（无 MCP）
- **测试**: desktop_mcp.py T-MCP-1 到 T-MCP-6

---

## 桌面模式 known-gap（Plan 370）

以下 D-GAP 条目记录桌面模式的已知限制。这些**不是测试失败**，而是
平台差异——Web 有但桌面（iced/VM）没有的特性。

### D-GAP-1: 语义 CSS token
- **Web**: bg-primary/text-foreground/bg-card/bg-muted/border-border 等生效（CSS 变量）
- **Desktop**: iced style parser 不识别 semantic token，全部静默丢弃
- **影响**: 桌面 UI 无颜色主题（默认黑白色）
- **根因**: `style/class.rs` 的 `from_tailwind` 只认 Tailwind 调色板，不构造 semantic Color 变体
- **修复方向**: 在 iced adapter 里映射 semantic token → Color 枚举的 Primary/Background 等

### D-GAP-2: dark mode
- **Web**: `:class="{ dark: store.dark_mode }"` + CSS 变量切换
- **Desktop**: 无实现（iced renderer 不读 dark_mode 状态）
- **影响**: 桌面无法切换暗色模式
- **修复方向**: iced renderer 监听 dark_mode 状态，切换 Color 调色板

### D-GAP-3: AutoDownEditor（Tiptap 富文本）
- **Web**: `<AutoDownEditor>` 挂载 Tiptap WYSIWYG 编辑器
- **Desktop VM**: `autodown_editor` tag 走 fallback → 渲染为 Empty
- **Desktop Rust**: a2r 不识别 `autodown_editor`（不在 KNOWN_TAGS）
- **影响**: 桌面无富文本编辑
- **修复方向**: 在 iced 渲染器或 a2r 中映射 `autodown_editor` → iced `text_editor`

### D-GAP-4: store composable  ✅ 已修复 (Plan 370)
- **Web**: `use store: NotesStore` → Vue reactive composable，跨组件共享
- **Desktop VM**: ✅ 已修复。store 的 state 字段（notes/active_id/active_folder/...）
  合并进 root widget 的状态对象；store 方法调用（`store.NewNote()`、
  `store.TogglePin(id)`、`store.SetAccent(name)` 等，含带参）经 codegen rewrite 为
  `handler_<StoreName>_<Method>` 并在合并 VM 中执行。VmRef 列表字段在 MCP
  snapshot/state 工具中 materialize 为内联 Array。
  - 验证：`plan370_store_vm_tests`（数据层：notes=6 种子数据）、
    `plan370_015_behavior_tests`（D1-D9 handler 行为：NewNote/SelectNote/
    TogglePin/SetAccent 等 8 项全绿）、`desktop_mcp.py`（真实 iced 窗口：点 New →
    notes 6→7，13 passed/0 failed）。
- **Desktop Rust**: ⚠️ a2r 仍不生成 store 代码（未在本计划范围）。
- **遗留限制（独立于 D-GAP-4）**: NavTree 侧边栏（sidebar.at）声明了顶层
  `view fn NoteRow/NoteItem` 片段；其解析路径不完整，导致 sidebar.at 整体解析
  失败、NavTree 在 VM 模式渲染为 FALLBACK（笔记列表项不显示）。这不影响 store
  数据层与 handler（App 的笔记编辑区、New 按钮、所有 handler 均正常），仅影响
  侧边栏列表渲染。已记录为新问题，待独立修复。

### D-GAP-5: accent 主题色
- **Web**: applyAccent 写 CSS 变量 + localStorage 持久化
- **Desktop**: 无实现
- **影响**: 桌面无主题色切换
- **修复方向**: 依赖 D-GAP-1（semantic token）先落地

---

## 维护约定

- 改 .at 功能时，**同步更新本契约**（加/改/删 T 条目）
- 新增生成器契约时，在"契约依据汇总"加行，并关联到对应 T
- 修复 bug 时，加一个**回归测试**条目（如 T12-DARK），标注"历史教训"
- 新增桌面特性或修复 D-GAP 时，**同步更新 D/D-GAP 条目**
