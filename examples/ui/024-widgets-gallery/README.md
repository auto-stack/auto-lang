# 024-widgets-gallery → 已并入 widgets-gallery

本示例已迁移至 **[`examples/widgets-gallery`](../../widgets-gallery/README.md)**
（原名 `examples/gallery`），作为 Auto 实现的 UI widgets gallery（全部 shadcn
widgets 的示例 + 文档，含 Plan 408 §9 的 7 个 codegen 修复验证）。

024 这个编号目录仅保留此指引，避免与 `examples/widgets-gallery` 内容重复漂移。

运行方式见 widgets-gallery 的 README：

```bash
cd examples/widgets-gallery
auto run        # 全流程构建 + dev server → http://localhost:3024/
```
