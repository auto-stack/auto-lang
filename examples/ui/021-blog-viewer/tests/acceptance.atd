# 021-blog-viewer 验收契约
# T1: 首页加载 (顶栏 "My Blog" + 计数 "6 articles" + 6 张卡片)
# T2: 标签过滤 (点 Rust chip → 只剩 2 张 Rust 卡；点 All 恢复 6 张)
# T3: 进入详情页 (点首卡 → URL `/post/:id`、标题/作者与卡片一致、正文分段 ≥3 段)
# T4: 点赞 clap (点赞 → 计数 +1；再点 → 回落)
# T5: 发布流程 (顶栏 Write → `/new` 表单填写 → Publish → 回列表且新卡置顶、计数变 7)
# T6: 发布校验 (空标题提交 → 错误提示出现、不跳转)
# T7: 删除流程 (详情页删除 → 确认条出现 → 确认 → 回列表、卡片消失、计数回落)
