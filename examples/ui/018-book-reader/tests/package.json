{
  "name": "book-reader-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Plan 399 acceptance tests for 018-book-reader (multi-route + rust backend)",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": { "@playwright/test": "^1.48.0" }
}
