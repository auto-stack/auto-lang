# 011-calculator 验收契约

> 本文件是 011-calculator 功能测试的 **single source of truth**。
> 每个 T 条目声明一个验收场景（领域语言），对应的 MCP 交互测试在
> `desktop_mcp.py` 里按同一 ID 实现（经 `autoui_press_sequence` 以真实
> 按键驱动，非直调计算）。
>
> **契约依据**引用 Plan 403 的需求条目（1a MCP 操纵 / 1b press_sequence /
> 1c 表达式引擎 / 3 多模式 / Phase 403-F VM 浮点修复）。

---

## 基础类

### T1: 界面结构
- **当** 应用以 VM 模式启动并完成首帧渲染
- **那么** 快照应含 App widget、aura 元素 ID、数字键 1-9、
  运算符 +/-/*//=，以及 Basic/Scientific/Programmer 模式切换页签
- **契约依据**：403 需求 2（grid 键盘重构）、需求 3（多模式）

### T2: 初始状态
- **当** 应用启动完成
- **那么** display 为 "0"、mode 为 "basic"、error 为空
- **契约依据**：403 表达式引擎初始态

---

## 求值类

### T3: 整数求值 2+3=5
- **当** 依次按键 [2, +, 3, =]
- **那么** display 应为 5
- **契约依据**：403 需求 1b（press_sequence 标志性用例）

### T4: 链式求值 1+2+3=6
- **当** 依次按键 [1, +, 2, +, 3, =]
- **那么** display 应为 6
- **契约依据**：403 需求 1b（链式运算验证）

### T5: 小数求值 3.5+1=4.5
- **当** 依次按键 [3, ., 5, +, 1, =]
- **那么** display 应为 4.5
- **契约依据**：403 需求 1c（小数支持）+ Phase 403-F（VM 浮点修复，e2e 经 MCP）

### T6: 科学模式括号与优先级 2*(3+4)=14
- **当** 按键 [Scientific] 切换模式（mode 应变为 scientific）
- **且** 依次按键 [2, *, (, 3, +, 4, ), =]
- **那么** display 应为 14
- **契约依据**：403 需求 1c（括号/优先级）+ 需求 3（模式切换为真实按键）

### T7: 清零
- **当** 按键 [7] 后再按 [C]
- **那么** display 应回到 0
- **契约依据**：403 键盘 Clear 行为

---

*运行方式：`cd examples/ui/011-calculator/tests && python desktop_mcp.py`
（需 ui-iced 版 auto 与 python requests；脚本自行拉起/回收 VM 进程）*
