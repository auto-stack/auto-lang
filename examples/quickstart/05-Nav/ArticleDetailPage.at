// ArticleDetailPage.at - Article detail page for navigation
// Note: ArticleDetailPage doesn't exist yet - will need to create file

use QuickStartPage:import ArticleDetailPage

widget ArticleDetailPage {
    @Consume("pathStack") pathStack: NavPathStack

    view {
        NavDestination() {
            Column {
                Text("Article Detail Page")
                    .fontSize(20)
                    .padding(20)
            }
        }
    }
}
