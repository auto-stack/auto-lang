fn main() {
    let nil = ListNode(data: 0, next: ListNode(data: 0, next: nil))

    let node3 = ListNode.new(30, nil)
    let node2 = ListNode.new(20, node3)
    let node1 = ListNode.new(10, node2)

    let current = node1
    let data1 = current.get_data()
    let next1 = current.get_next()

    let data2 = next1.get_data()
    let next2 = next1.get_next()

    let data3 = next2.get_data()

    data1
}
