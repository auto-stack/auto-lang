// Test borrow expression parsing

// View expression
let v = view x

// Mut expression
let m = mut y

// Take expression
let t = take z

// Nested borrow expressions
let v2 = view (mut x)

// Borrow with function calls
let result = view str_len(s)
