tag Atom {
    Int int
    Float float

    fn is_int() bool {
        is self {
            Atom.Int(_) => true
            _ => false
        }
    }
}

fn main() {
    let a = Atom.Int(42)
    let result = a.is_int()
    result
}
