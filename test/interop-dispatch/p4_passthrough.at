fn fib(n int) -> int {
    if n < 2 { return n }
    return fib(n-1) + fib(n-2)
}
fn main() {
    var xs = [3, 1, 2]
    for x in xs { print(fib(x)) }
    print("sum: " + (xs[0]+xs[1]+xs[2]).to(str))
}
