// == Comptime Example: 条件编译链 ==
//
// 描述：展示使用运行时 if/else 链进行条件判断
// 注意：#if 是编译期条件，需要 CTEE 支持
// 当前示例使用运行时条件作为替代

// #[expect_value("medium")]
fn main() {
    let level = 2

    if level == 1 {
        "low"
    } else if level == 2 {
        "medium"
    } else {
        "high"
    }
}
