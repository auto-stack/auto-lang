// == Comptime Example: 循环与条件组合 ==
//
// 描述：展示 for 循环与 if 条件组合使用

// #[expect_value(9)]
fn main() {
    // 计算 1-5 中奇数的和 (1 + 3 + 5 = 9)
    var sum = 0
    for i in 1..6 {
        if i % 2 == 1 {
            sum = sum + i
        }
    }
    sum
}
