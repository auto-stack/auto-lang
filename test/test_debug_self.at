// Debug test for self binding in type methods

type Point {
    x int
    y int

    fn get_x() int {
        return 99
    }
}

fn main() {
    let p = Point { x: 42, y: 10 }
    let result = p.get_x()
    print(result)  // Expected: 99 (for now)
}
