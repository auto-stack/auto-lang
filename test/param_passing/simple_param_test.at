// Simple parameter test without view/copy keywords
type Point {
    x int
    y int
}

fn process_point(p Point, scale int) int {
    return p.x * scale
}

fn main() void {
    let pt = Point{x: 10, y: 20}
    let result = process_point(pt, 2)

    say("Result:")
    say(result)
}
