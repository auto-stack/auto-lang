// plan-429 B2 性能摸底:use.rust 密集循环(Vec)
// VM 模式: auto bench.at;a2r 模式: auto trans --path bench.at rust 后 rustc 编译运行
use.rust std::vec::Vec

fn main() {
    var v = Vec.new()
    var i = 0
    while i < 100000 {
        v.push(i)
        i = i + 1
    }
    print(v.len())
}
