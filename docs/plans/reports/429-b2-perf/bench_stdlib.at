// plan-429 B2 对照组:同样循环用 Auto 自身 stdlib 的 List(走 LIST_* opcode,非 dispatch 3000)
fn main() {
    var v = List.new()
    var i = 0
    while i < 100000 {
        v.push(i)
        i = i + 1
    }
    print(v.len())
}
