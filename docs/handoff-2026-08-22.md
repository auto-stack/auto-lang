# 交接说明：auto-lang 计划审计与修复工作流（2026-08-22）

## 背景与使命

对 docs/plans/ 的未归档计划做代码级完成度审计（不信计划自述、以 master 实测为准），
然后按 backlog 逐项用 worktree 修复、测试全绿后合并 master。已完成七批修复 + 多轮归档。
**快赢通道已基本清空**——剩余项均为大件（需拆粒度立项）或物理/视觉验证项。

**核心档案（先读这三个文件）：**
1. `docs/plans/plans-status-audit-2026-08-20.md` —— 审计主文档，**§5 是工作队列**
   （A1-A8/B1-B12/G1-G4 分层，含状态与合并 commit 号；B 线除 B5/B7/B8 外全关）
2. `docs/plans/KNOWN-DEBT-AND-RISKS.md` —— 债务登记簿（风险/绕道 + "延期"段）
3. `docs/plans/396-a2r-auto-ai-related-improvements.md` —— **已归档**（§2.1-§2.6 六条全根治）

## 当前状态（截至 auto-lang `595a79a8`，已推双远端）

**专项交接文档(2026-08-22 晚,供两个新会话分别开工)**:
- **E3 有界泛型** → `docs/handoff-E3-bounded-generics.md`(双断点精确定位:
  泛型表拒 `has` 语法[TypeParam.constraint 字段已在] + 泛型接收者方法调用静态
  解析[CALL_SPEC 运行时分派机制已在,改走即可];a2r 裸 `<T>` 发射双错;
  P1-P5 分阶段路径 + 验收探针)
- **E2 关联类型** → `docs/handoff-E2-associated-types.md`(语言级四层特性:
  语法[SpecDecl 加 associated_types] → trait_checker 绑定替换 → VM 合成管线
  文本替换[a2r 映射 Rust 原生 type Item/Self::Item];P1-P5 + 绑定语法建议
  命名式 Container<Item=int>)
- 两案互不依赖,可并行;工作流/陷阱共用 E3 文档 §0

## 当前状态（截至 auto-lang 本批，已推双远端）

**第五会话（2026-08-22 傍晚,同步 + if 表达式根治 + E3 定位）**：
- **同步**:master 并行会话 debug 合并(a0444f71,host.call 失败打点)推双远端
- **a2r 值位尾 if 表达式根治**（`6c454f19`）:body() is_returnable 纳入带
  else 的 Stmt::If + value_if_tail 旗标(then/else/else-if 臂与嵌套尾免分号,
  语句上下文 Plan 393 E3 零变化);golden 013_if_tail_value;**trait_advanced
  三方 10/10 全绿**(镜像错→E1 修 runner→暴露 if 缺口→本批根治,三层剥洋葱
  完整闭环);KNOWN-DEBT 417-E4 调查条翻转
- **417-E3 精确定位**(探针双断点):①泛型参数表拒 `has` 语法;②裸 `<T>`
  方法调用静态解析 → "Undefined symbol: T.compare"——需单态化或 spec 动态
  分派,多天级语言特性,分歧条已更新实施路径
- **Phase E 现况**:E1✅ E5✅ E4✅;剩 E3(多天级)与 E2(关联类型)
- parity 全景:string_utils 22/22 + generators 6/6 + http_client_sync 5/5 +
  trait_advanced 10/10 四库全绿

## 当前状态（截至 auto-lang `abc03b3a`，待推）

**第四会话新增（2026-08-22 深夜续,归档批 + 416/417 收官推进）**：
- **第二批归档**（`439cffaa`）:**402/346/411/243 四计划全部完成归档**
  (状态行刷新+债务登记 §411×2/§243×2+索引/报告同步);审计注明第二批
- **416-6C**（`6cf5d15e`）:全仓 fmt 决议为**包级增量清零政策**(a2r-std
  本批清零并入 CI);F5 手动核验步骤写入 416 文档——**416 全条目完,
  仅剩一次 10 分钟手动 F5**(步骤:§5 尾注)
- **417-E4**（`abc03b3a`）:**DIV-TRAIT-VM-2 根治**——spec 默认方法继承
  (trait_checker 放行默认体 + codegen 经 TypeStore 合成未重声明的默认
  方法,重声明=覆盖);a2r 侧本就正确;**Phase E 五项已关三**(E1/E5/E4),
  剩 E2(关联类型,语言级)与 E3(有界泛型)
- 新登记债务:417-E4 调查条(trait_advanced 三方仍阻于预存 a2r if 表达式
  尾臂分号缺口,此前被镜像错误掩盖)
- 未归档计划现状:415(仅 A 完成)/416(待 F5)/417(剩 2 项)/330/332/386/
  394(显式暂缓)/400/401/405/407/412/413/414(各有所剩,见审计)

## 当前状态（截至 auto-lang `f599dde2`，待推）

**深夜会话新增（2026-08-22,416 收官 + 417 重大进展,四支全部 worktree→合并）**：
- **416-5C**（`b6decb10`）:`Token::all_keywords()` lexer 权威关键字表(56 词,
  round-trip 锁定)+ stdlib_index 惰性解析补全(`json.` 成员/类型位模块名)+
  docs/lsp-api-contract.md 契约文档
- **416-6B**（`0a30fafa`）:LSP 协议级集成测试(LspService 直驱 + ClientSocket
  排水)——生命周期/增量 didChange/多文档/rename 边界(实证单文档作用域)
- **416-5B**（`7727c657`）:semantic tokens 服务端(词法+AST 双源分类,8 类型
  legend,协议级 round-trip);**416 仅剩 VSCode F5 着色核验(手动)+ 6-C
  全仓 fmt 决策**
- **417-E5/D3**（`f599dde2`）:**DIV-HTTP-LANG-1 验证翻转**——http.at 解析
  阻塞已被后续 parser 批次修复("文档滞后于代码"重演),**http_client_sync
  三方 5/5(连跑三次稳定),D3 解锁全绿**;Phase E 现况:E1✅ E5✅,剩
  E2(关联类型,语言级)/E3/E4(trait VM 两项)
- auto-lsp 17 测试全绿(6→9 lib + 4 协议);417 E5 为 0 代码纯验证批

## 当前状态（截至 auto-lang `aeafc5a7`，auto-vscode `fc4cb9d`，均已推远端）

**晚间会话新增（2026-08-22,416/417 计划开工批,五支全部 worktree→全绿→合并→推送）**：
- **415-A `HashMap::from` 发射**（`9c678a03`）:Map 类型+对象字面量 →
  HashMap::from(str 键/值包 String::from);golden 006_map_literal 新增,
  003/006_map_type 旧非法输出双修;a2r 341/341;跨仓环三 retranspile 零错
- **417-E1 CHAR-AT**（`aff61954`）:char_at 推断从 String 臂移入 Int;
  golden 007_char_at_infer;string_utils 四处 workaround 注解移除,DIV 翻
  fixed;顺带修 a2r-std StringBuilder i32→i64 漂移 + parity runner
  async-stream 连字符(aliyun 镜像兼容)
- **417-D2 generators 三方 parity**（`737e1d5f`）:新 parity 库 6/6 一致;
  **VM 带参生成器根治**(CREATE_GENERATOR/CALL 短路三处创建点统一搬参,
  带参生成器此前从未工作——counter(10,3) 由 RET 下溢 panic 修为正确 33);
  **a2r ~Iter 统一降级 impl futures::Stream**(旧 impl Iterator 壳从不兼容);
  **register_import_signatures**(use_stmt 时解析 ./auto/<mod>.at 登记导入
  函数签名)——string_utils 三方 22/22 恢复全绿,**E1b 预存断裂连带根治**
- **416-5A extension TS 迁移**（auto-vscode `fc4cb9d`,已推 gitee）:
  src/extension.ts 全量类型化 + webpack→esbuild;tsc --noEmit 零错;
  新旧 bundle 宿主外冒烟等价
- **416-6A LSP CI 解红**（`aeafc5a7`）:常红根因=fmt 查全仓(9071 文件);
  auto-lsp 包级 fmt 清零 + push/pull_request 触发恢复(paths 过滤)
- 回归基线全程保持:a2r 343/343、默认 lib 3048/1、ui-iced 3485/8、
  auto-man 220/0、实机 013 22/0/0 + 038 21/0 + 015 13/0/1

## 当前状态（截至 auto-lang `1fd16ea7`，auto-ai `5d0548b`，auto-lang 已推双远端）

**本轮新增（2026-08-22 下午会话，四支全部合并 master + B5/B7/B8 立项）**：
- **G4 视觉像素三项收官**（`plan-fix/g4a-visual` 535b291d/d2748a24，合并 7cf2f4ce）：
  P1-C Inter 三字重内嵌（OFL）+ 5 入口 default_font；P2-A① 色板对齐 prism-tomorrow
  （vue 实际加载主题）+ function/operator/boolean 三类别；P2-A④ 表头 medium/muted、
  行 border-b、单元格 px-4/py-3。**411 仅剩 §8.5 声明保留项，视觉并排截图可后补**
- **api_gen rust 路径 header 注入**（`plan-fix/b1c-headers` 711ec24d，合并 73e80207）：
  `meta str` 参数按 VM push_meta 同约定注入（HeaderMap + meta_json 绑定），
  Authorization/cookies 进 rust 后端——**审计 B1(b) 关闭**
- **D-GAP-4 子组件事件剥离根治**（`plan-fix/dgap4-ctrack` 0f6a3eb7，合并 1fd16ea7）：
  三连根因（render_child_widget 无跟踪 build / Expr::FStr 无求值臂→文本节点丢→兄弟
  位移 / tracked row 条件拼接同路径）；**013 实机 16/0/4 → 22/0/0**（T3-T5 真点击），
  顺带修 master 预存 ui-iced 测试编坏（Plan 060 ShellExecRequest 字段）
- **B5/B7/B8 拆粒度立项**：Plan 415（242 剩余五子项）/416（243 Phase 5-6，TS 迁移+
  semantic tokens+CI）/417（359 residuals，Phase E 按 DIV 依赖排序）——审计 §5.2 三行
  均已回填"已立项"
- 合并态验证：ui-iced 3485/8（基线）、默认 lib 3048/1（环境项）、auto-man 220+6、
  实机 013 22/0/0 + 038 21/0/0 + 015 13/0/1

**auto-lang 侧**（此前批次，origin+gitee 双远端同步）：
- **396 §2.5+§2.6**（合并 1ae3b33c/283990bc）：三段限定 unit-variant 模式剥模块段
  （此前两轮插桩扑空——AST 实为 Dot(TagCover)，a2r 由 Cover 裸分支+字段访问拼出）；
  a2r_std time i32→i64 对齐 stdlib 声明
- **BOM panic**（1fb96eac）：lexer 跳过 U+FEFF，带 BOM 的 .at 不再在 parser.rs:272 崩溃
- **B10(a)**（4dc2964f）：read_all_state_materialized 物化 Int 句柄 List（015 notes 计数断言复活）
- **B10(b)**（7ef6f713）：013 desktop_mcp.py 按 TodoMVC 真实语义重写（12→16 过/0 败）
- **B12 全案**（695b4736/41ac939/a7fe6e9d）：(i) 幻影实参帧错位（无参 handler 被塞
  input_value → call_fn n_args=2 vs prolog=1）；(ii) renderer 内嵌史前 Todo 应用双重执行
  （backtrace 定案，legacy 臂 !has_handler 门控）；(a) GET_FIELD 实例 bool 裸 i32 +
  iced boot 零值碾碎 Init 计数。013 实机 12→16 过/0 败
- **B6 全案**：Request-ID + Rate Limit（catalog 3109）+ multipart 上传
  （7f26c50a：RFC 2046 二进制安全 + Content-Length 续读 + AUTO_UPLOAD_DIR 落盘；
  拦路虎=content_type 头小写化毁边界大小写）
- **B1 核心**（7f2faaa2）：023 真 token 认证（Cred 凭证/验密码/递增唯一令牌/
  bearer_token 双形态/create_article 作者由 token 推导）；e2e 六段全绿
- **str-parity**（9a01ae64）：substr(len)/slice+sub(end) 双 shim 分离 + find 锁定
- **G3**（e8eb3624）：038 实机 UTF-8 panic 根治（highlight_code 按字节切多字节标签）+
  desktop_mcp 21/0（洪水填充/数字显示/胜负全闭环）
- **G4 P2-B 全五项**：Button.content vtree 序列化（f7658c45）/ check 假阳性清零 +
  快照过滤 Empty（6fbcbe40）/ 截图守卫盘点（已有）/**layout 回填**（30fc2d08：
  vnode 路径哈希 id + plain 路径丢 id 补全 + 双前缀 collector + 直解析——容器骨架
  真实绝对坐标全出；真缺口=apply_column/row_style 无样式分支丢 widget_id）

**auto-ai 侧**（origin 已同步；**gitee 404 无此仓**，交接说明曾误记"已推 gitee"）：
- `31a5304`：ai-config unit-variant sed 毕业（§2.5）
- `e05e48d`：client 重生成归零（§2.6 i64 对齐）——三转译 crate 首次同时全绿

## 当前状态（截至 auto-lang 本批 417-E2，分支 plan-fix/417e2-assoc-types，合并后推双远端）

**本轮新增（2026-08-22 E2 会话，spec 关联类型四层落地，DIV-TRAIT-LANG-1 翻转）**：
- **417-E2**（`plan-fix/417e2-assoc-types`，5 批提交）：**语言级关联类型特性**
  落地——①parser/AST：`spec Container { type Item }` 成员入
  SpecDecl.associated_types（名字入 current_type_params，签名引用解析为裸
  Type::User）；实现处命名式绑定 `as Container<Item=int>` 入 SpecImpl.
  assoc_bindings（`<` 内 peek `=` 与位置式 type_args 区分，两者可共存）。
  ②trait_checker：check_conformance 按绑定 Type::substitute 替换签名比对，
  未绑定/未知绑定名显式报错（+4 单测）。③VM：关联类型无运行时实体，
  TypeDecl 编译臂对声明方法与 E4 合成默认方法均做编译期替换（探针
  `IntBox as Container<Item=int>` 输出 8/7 与基线一致）。④a2r：trait 发射
  `type Item;` + 签名引用重写 `Self::Item`（复用 substitute，嵌套位置
  List<Item> 同覆盖）；impl 块首发射 `type Item = i64;`；修
  rust_return_type_name 对 Self:: 名误加 impl 前缀（`-> impl Self::Item`）。
  ⑤parity：trait_advanced sub-scenario B **L3→L1**，三方 **14/14**（基线 10/10）。
- 回归全绿：a2r golden **345/345**（基线 344+新增 011_associated_types，产物
  独立 cargo 编译输出 8/7）；全量 lib **3055/1**（唯一败=已知环境项
  route::discovery）；实机 038 21/0、013 22/0/0、015 13/0/1；parity 四库
  string_utils 22/22、generators 6/6、http_client_sync 5/5（首跑 4/5 为已知
  mock-server 竞态，复跑稳定）、trait_advanced 14/14
- 新登记债务：a2r `fix_vec_i32_index` 后处理正则把**任意** `xxx.get(i)`（i ∈
  int_like 名单）盲重写为 `xxx[i as usize]`——用户类型带 `.get(int)` 方法且
  局部变量名不在 hash_map_names 白名单时产物编坏（与 E2 无关的既有启发式，
  parity wrapper 以变量名 `data` 规避；见 KNOWN-DEBT-AND-RISKS 417-E2 条）
- **Phase E 现况**：E1✅ E5✅ E4✅ E2✅，**仅剩 E3**（有界泛型，见
  docs/handoff-E3-bounded-generics.md，姊妹 worktree 进行中）——417 待 E3
  落地后进入 finish-plan 评估

## 下一步建议（按优先级，2026-08-22 下午更新）

1. **Plan 415/416/417 按序开工**（本轮立项，入口即各计划文档）：
   - 415-A `HashMap::from` 发射（最小、独立）→ 415-E dep cc → 415-B1 SQLite
   - 416 5-A TS 纯迁移（无联调）→ 6-A CI 解红 → 5-B semantic tokens（唯一 VSCode 实机点）
   - 417 E1 CHAR-AT 最小 → D2 generators（不依赖 Phase E 可并行）
2. **G 类残余**：G1（412 Layout gallery 双端 ≤1px 截图——需浏览器）、G2（413 IME/DPI/
   Linux 物理项）；411 Inter 与 vue 并排字形截图人工核对（非阻塞）
3. 审计 §5.2 仅剩 B1(c)（playwright 登录流/401 语义化）与 B2（纯 module fn codegen，
   低优先）两行开放；§5.1 A 线全关
4. 346 的 A5 redirect 已由早批落地后，346 未实施项清零——可评估归档 346
   （含 403/401 等部分完成计划的 finish-plan 轮回）

## 工作规范（沿用既有模式）

- **Worktree 流程**：`git worktree add .worktrees/plan-fix-XXX -b plan-fix/XXX master`
  → 修复+测试 → `git merge --no-ff` 回 master → 删 worktree → 推 origin+gitee 双远端
- **测试命令**：
  - a2r golden：`RUST_MIN_STACK=33554432 cargo test -p auto-lang --lib --features test-trans a2r_tests`（基线 340/340）
  - 全量 lib：`cargo test -p auto-lang --lib`（3048 过，`route::discovery::tests::test_exists` 失败为已知环境项）
  - ui-iced 门控测试：`--features ui-iced`（vm_bridge/dynamic/plan370 等在默认构建不编译）
  - http e2e：`--features test-http-e2e -- --test-threads=1`
  - 实机 desktop_mcp：`cd examples/ui/XXX/tests && AUTO_BIN=<exe> python desktop_mcp.py`
    （已覆盖 011/013/015/038 四例）
- **归档流程**（finish-plan → archive-plan）：代码级复核 → 延期项登记债务簿 → 状态行 ✅ →
  git mv archive/ → 更新 plan-indices/plan-reports → 提交推送
- **跨仓验证环**（a2r 改动必做）：worktree 构建 → auto-ai 三 retranspile + cargo check 零错
- **全绿纪律**：不达标不合并——宁可撤回 worktree 登记发现（P2-B #1 前两轮即此模式）

## 环境陷阱（踩过的坑，重要）

- **python 补丁必须二进制安全**：`open(p,'rb').read().decode()` + `open(p,'wb').write(encode())`
  ——文本模式会重写整个文件的行尾（git diff 爆 12000+ 行）；renderer.rs 是 CRLF，
  layout_collector.rs 是 LF，**多行锚点匹配前先查行尾**
- **Git Bash heredoc 吃转义**：复杂补丁用 Write 工具写 .py 文件再执行
- **CALL_SPEC 实参序**：栈顶是**最后一个**实参（弹参从末参开始）
- **native catalog 编号**：新函数必须全局唯一新号（3108/3109 已占）
- **VM 模式 body 恒为单参数**（整个 JSON 串）——多字段解析走 json_str 提取器（023 api.at）
- **render_support 静态表**须与 aura_view_builder 实际 tag 分支同步（否则 autoui_check 假阳性）
- **已知预存测试失败**（勿误判回归）：lib 的 route::discovery test_exists（环境项）；
  ui-iced 下 plan370 d3/d4/d6/d7 + calendar ×2 + style test_sizing（基线一致）；
  e2e_notes_crud 顺序竞态 + concurrent_sse flake（文档化）
- **auto-ai 有并行会话**（PLAN-030/033）：auto-lang 主 worktree 的
  `examples/rust-workspace/Cargo.toml`（plan-032-back）是并行 WIP，勿动勿提交
- 既有 worktrees（auto-down/auto-edit-dev/auto-edit-ux/auto-shell）是并行会话工作流，勿删
- a2r 套件必须 `RUST_MIN_STACK=33554432`；僵尸 auto.exe 占端口用 AUTOUI_MCP_PORT 免疫
- **D 盘易满**：每个 worktree 的 target 独立（~10GB+），分支合并后立即
  `git worktree remove <path> --force` 释放；linker 报 `no space on device` 即此因
- **cargo test -p auto-man 会再生成 rust-workspace 三文件**（015 main.rs/
  Cargo.toml/.cargo/config.toml）——提交前剔出（`git checkout -- examples/rust-workspace/`），
  g4a/b1c 批次曾险些混入
- **master 曾有预存编坏**（Plan 060 改 ShellExecRequest 字段未更新测试，ui-iced
  测试无法编译）——已随 dgap4 批修复（0f6a3eb7）；后续遇 ui-iced 测试编译失败先查此因
- **主仓 target/debug/auto.exe 可能过期**——跨仓验证前先在 worktree 构建

## 近期关键 commit（溯源用）

2026-08-22 下午：1fd16ea7（D-GAP-4 合并）→ 0f6a3eb7（dgap4 三连修复）→
73e80207（B1b api_gen header 合并）→ 711ec24d（meta 注入）→ 7cf2f4ce（G4a
视觉合并）→ d2748a24（Inter 内嵌）→ 535b291d（Prism 色板+表格）；并行会话：
189800a5（auto-shell 合并）→ b6303f07（Plan 060 M1+M2）
此前：auto-lang：54f022ba（G4e layout 回填）→ 30fc2d08 → c8ae053a（P2-B#1 二轮）→ 82f50335
（一轮）→ ba0d8cfc（G4b check 对齐）→ 4120321d（G4 Button.content）→ 0d190a73（G3
扫雷）→ ddb8d25f（str-parity）→ 621c8f2e（B1 token auth）→ 95490c90（B6 multipart）→
205a6f41（B12a）→ 8b3cb1cc（B12ii）→ 695b4736（B12i 幻影实参）→ 1ae3b33c（§2.5）→
3a09960f（396 归档）→ 283990bc（§2.6）
auto-ai：5d0548b（并行会话 PLAN-033）→ e05e48d（client 归零）→ 31a5304（ai-config sed 毕业）

## Plan 417-E3 有界泛型（第六会话，worktree plan-fix-417e3-bounds）

**DIV-TRAIT-VM-1 翻转**：`fn max_of<T has Comparable>(a T, b T) T` 三端全通
（VM 探针输出 9；a2r 独立编译输出 9；parity trait_advanced 三方 14/14）。

- **P1 语法**：`parse_generic_param` 加 `TokenKind::Has` 分支（多 bound
  `has A + B`）写入既有 `TypeParam.constraint`。附带根因修复：无注解路径
  `fn_decl_stmt` 此前把 `<T>` 解析后丢弃（不写 `fn_expr.type_params`），
  已补齐与注解路径一致的传递——这是 a2r"泛型声明丢失"的真因。
- **P2 VM 分派**：codegen 新增 `current_fn_type_params` 追踪；接收者静态类型
  是当前 fn 泛型参数时方法调用改发 CALL_SPEC（运行时堆 tag 分派）。途中根治
  两个隐藏 bug：①CALL_SPEC 分派 VM 函数未压 CallFrame → 被调方 RET 弹掉调用
  者帧 → `current_fn_n_args` 污染 → 同表达式第二个 CALL_SPEC 的参数 load 落
  null 保护（既有 `is_spec_dispatch` 用户同样受益）；②链接期字符串池合并的
  `remap_string_indices` 步行器把 CALL_SPEC 按 4 字节跳过（实际 3）且不重映射
  method idx → 跨模块 CALL_SPEC 分派错误方法名（disasm 解码表/opcode 注释
  一并修正）。
- **P3 a2r**：`<T: Comparable>` 经 parser 贯通自动发射；`rust_return_type_name`
  /`rust_type_name` 的 User 分支对当前 fn 泛型参数直接返回裸名（修 `-> impl T`）。
- **golden**：a2p 003/004 期望更新（泛型参数注解按 `is_generic_param` 设计
  剥除——旧期望固化的是 type_params 丢失行为）；a2r 002/005 期望更新（同因）；
  新增 08_generics/008_bounded_generic_fn（a2r 345/345）。
- **P4 界校验**：评估后按"缩小单批范围"纪律延后，登记 KNOWN-DEBT（417-E3-P4）。
- **P5 parity**：库加 `max_of<T has Comparable>` + 反向 compare 第二实现者
  ScoreDesc（证明按接收者类型分派）；bounded_generics.at 4 例 + Rust oracle
  同名；README/known-divergences/417 计划行同步翻转。
- **回归**：lib 3054/1（唯一败=已知环境项 test_exists）；a2r golden 345/345；
  parity 四库全绿（trait_advanced 14/14、string_utils、generators、
  http_client_sync 5/5——首跑 1 例 Rust oracle 冷编译竞态，复跑稳定）；
  实机 038(21/0)/013(22/0/0)/015(13/0/1) 与基线一致。
