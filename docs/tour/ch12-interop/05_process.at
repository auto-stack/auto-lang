// Process — Running external commands
//
// Auto can spawn external processes via use.rust std::process.

fn main() {
    print("Process operations available via use.rust:")
    print("  std::process::Command for spawning processes")
    print("  Capture stdout/stderr/exit codes")
    print("Use dep + use.rust to access process APIs")
}
