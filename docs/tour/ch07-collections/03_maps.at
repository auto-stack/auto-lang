// Maps — Key-value Map<K,V> type
//
// `Map` stores key-value pairs.

fn main() {
    // Object literal as key-value store
    let config = { host: "localhost", port: "8080" }
    print(config.host)
    print(config.port)
}
