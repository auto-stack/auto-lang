[package]
name = "ime-spike"
version = "0.1.0"
edition = "2021"

# Plan 452 T6 一次性验证原型：独立于 auto-lang workspace，不进产品代码路径。
[workspace]

[dependencies]
# 与主 workspace 完全一致的版本与 feature 集，最大化共享 target 缓存
iced = { version = "=0.14.0", features = ["tokio", "image", "svg", "advanced", "canvas"] }
