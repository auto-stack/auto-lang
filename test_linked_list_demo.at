fn main() {
    let node3 = ListNode(data: 30, next: nil)
    let node2 = ListNode(data: 20, next: node3)
    let node1 = ListNode(data: 10, next: node2)

    let first_data = node1.get_data()
    let first_next = node1.get_next()
    let second_data = first_next.get_data()

    second_data
}
