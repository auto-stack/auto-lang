# auto

fn print(n T) {
    __print(n)
}

# c

use c <stdio.h>

fn print(n T) {
    $is T {
        int => printf("%d\n", n)
        str => printf("%s\n", n)
        float => printf("%f\n", n)
    }
}
