// Generated by shim-metadata (plan-430). DO NOT EDIT BY HAND.
// Regenerate: cargo run -p shim-metadata -- std-emit > this file
#[allow(unused_variables, clippy::all)]
pub fn generated_std_dispatch(
    type_name: &str,
    method: &str,
    task: &mut crate::vm::task::AutoTask,
    vm: &crate::vm::engine::AutoVM,
) -> Result<Option<()>, crate::vm::engine::VMError> {
    use crate::vm::ffi::convert::VMConvertible;
    use crate::vm::ffi::rust_stdlib::RustStdlibObject;
    use crate::vm::ffi::stdlib::push_rust_obj;
    use crate::vm::engine::VMError;
    fn ferr(ctx: &str, e: impl std::fmt::Display) -> VMError {
        VMError::RuntimeError(format!("{ctx}: {e}"))
    }
    match (type_name, method) {
        ("String", "new") => {
        let __r = String::new();
        let __s = __r.to_string();
        let idx = vm.add_string(__s.into_bytes());
        vm.rc_push_str_idx(task, idx);
            Ok(Some(()))
        }
        ("String", "from") => {
        let a0: String = String::pop_from_stack(task, vm).map_err(|e| ferr("String.from pop", e))?;
        let __r = String::from(&a0);
        let __s = __r.to_string();
        let idx = vm.add_string(__s.into_bytes());
        vm.rc_push_str_idx(task, idx);
            Ok(Some(()))
        }
        ("Vec", "new") => {
        let __r = Vec::<i64>::new();
        push_rust_obj(task, vm, "Vec", __r)?;
            Ok(Some(()))
        }
        ("Vec", "push") => {
        let a0: i64 = i64::pop_from_stack(task, vm).map_err(|e| ferr("Vec.push pop", e))?;
        let __r = {
            let handle = task.ram.pop_i32() as u64;
            let Some(obj) = vm.get_heap_object(handle) else {
                return Err(ferr("Vec.push", "bad handle"));
            };
            let mut guard = obj.write().unwrap();
            let Some(ro) = guard.as_any_mut().downcast_mut::<RustStdlibObject>() else {
                return Err(ferr("Vec.push", "not RustStdlibObject"));
            };
            let Some(recv) = ro.downcast_mut::<Vec<i64>>() else {
                return Err(ferr("Vec.push", "downcast failed"));
            };
            recv.push(a0)
        };
        task.ram.push_i32(0);
            Ok(Some(()))
        }
        ("Vec", "pop") => {
        let __r = {
            let handle = task.ram.pop_i32() as u64;
            let Some(obj) = vm.get_heap_object(handle) else {
                return Err(ferr("Vec.pop", "bad handle"));
            };
            let mut guard = obj.write().unwrap();
            let Some(ro) = guard.as_any_mut().downcast_mut::<RustStdlibObject>() else {
                return Err(ferr("Vec.pop", "not RustStdlibObject"));
            };
            let Some(recv) = ro.downcast_mut::<Vec<i64>>() else {
                return Err(ferr("Vec.pop", "downcast failed"));
            };
            recv.pop()
        };
        push_rust_obj(task, vm, "Option", __r)?;
            Ok(Some(()))
        }
        ("Vec", "len") => {
        let __r = {
            let Some(obj) = vm.get_heap_object(task.ram.pop_i32() as u64) else {
                return Err(ferr("Vec.len", "bad handle"));
            };
            let guard = obj.read().unwrap();
            let Some(ro) = guard.as_any().downcast_ref::<RustStdlibObject>() else {
                return Err(ferr("Vec.len", "not RustStdlibObject"));
            };
            let Some(recv) = ro.downcast_ref::<Vec<i64>>() else {
                return Err(ferr("Vec.len", "downcast failed"));
            };
            recv.len()
        };
        task.ram.push_i64(__r as i64);
            Ok(Some(()))
        }
        ("Vec", "is_empty") => {
        let __r = {
            let Some(obj) = vm.get_heap_object(task.ram.pop_i32() as u64) else {
                return Err(ferr("Vec.is_empty", "bad handle"));
            };
            let guard = obj.read().unwrap();
            let Some(ro) = guard.as_any().downcast_ref::<RustStdlibObject>() else {
                return Err(ferr("Vec.is_empty", "not RustStdlibObject"));
            };
            let Some(recv) = ro.downcast_ref::<Vec<i64>>() else {
                return Err(ferr("Vec.is_empty", "downcast failed"));
            };
            recv.is_empty()
        };
        task.ram.push_nv(auto_val::encode_bool(__r));
            Ok(Some(()))
        }
        ("Vec", "clear") => {
        let __r = {
            let handle = task.ram.pop_i32() as u64;
            let Some(obj) = vm.get_heap_object(handle) else {
                return Err(ferr("Vec.clear", "bad handle"));
            };
            let mut guard = obj.write().unwrap();
            let Some(ro) = guard.as_any_mut().downcast_mut::<RustStdlibObject>() else {
                return Err(ferr("Vec.clear", "not RustStdlibObject"));
            };
            let Some(recv) = ro.downcast_mut::<Vec<i64>>() else {
                return Err(ferr("Vec.clear", "downcast failed"));
            };
            recv.clear()
        };
        task.ram.push_i32(0);
            Ok(Some(()))
        }
        ("Vec", "get") => {
        let a0: _ = i64::pop_from_stack(task, vm).map_err(|e| ferr("Vec.get pop", e))? as usize;
        let __r = {
            let Some(obj) = vm.get_heap_object(task.ram.pop_i32() as u64) else {
                return Err(ferr("Vec.get", "bad handle"));
            };
            let guard = obj.read().unwrap();
            let Some(ro) = guard.as_any().downcast_ref::<RustStdlibObject>() else {
                return Err(ferr("Vec.get", "not RustStdlibObject"));
            };
            let Some(recv) = ro.downcast_ref::<Vec<i64>>() else {
                return Err(ferr("Vec.get", "downcast failed"));
            };
            recv.get(a0).copied()
        };
        push_rust_obj(task, vm, "Option", __r)?;
            Ok(Some(()))
        }
        ("Vec", "first") => {
        let __r = {
            let Some(obj) = vm.get_heap_object(task.ram.pop_i32() as u64) else {
                return Err(ferr("Vec.first", "bad handle"));
            };
            let guard = obj.read().unwrap();
            let Some(ro) = guard.as_any().downcast_ref::<RustStdlibObject>() else {
                return Err(ferr("Vec.first", "not RustStdlibObject"));
            };
            let Some(recv) = ro.downcast_ref::<Vec<i64>>() else {
                return Err(ferr("Vec.first", "downcast failed"));
            };
            recv.first().copied()
        };
        push_rust_obj(task, vm, "Option", __r)?;
            Ok(Some(()))
        }
        ("Vec", "last") => {
        let __r = {
            let Some(obj) = vm.get_heap_object(task.ram.pop_i32() as u64) else {
                return Err(ferr("Vec.last", "bad handle"));
            };
            let guard = obj.read().unwrap();
            let Some(ro) = guard.as_any().downcast_ref::<RustStdlibObject>() else {
                return Err(ferr("Vec.last", "not RustStdlibObject"));
            };
            let Some(recv) = ro.downcast_ref::<Vec<i64>>() else {
                return Err(ferr("Vec.last", "downcast failed"));
            };
            recv.last().copied()
        };
        push_rust_obj(task, vm, "Option", __r)?;
            Ok(Some(()))
        }
        ("Vec", "insert") => {
        let a0: _ = i64::pop_from_stack(task, vm).map_err(|e| ferr("Vec.insert pop", e))? as usize;
        let a1: i64 = i64::pop_from_stack(task, vm).map_err(|e| ferr("Vec.insert pop", e))?;
        let __r = {
            let handle = task.ram.pop_i32() as u64;
            let Some(obj) = vm.get_heap_object(handle) else {
                return Err(ferr("Vec.insert", "bad handle"));
            };
            let mut guard = obj.write().unwrap();
            let Some(ro) = guard.as_any_mut().downcast_mut::<RustStdlibObject>() else {
                return Err(ferr("Vec.insert", "not RustStdlibObject"));
            };
            let Some(recv) = ro.downcast_mut::<Vec<i64>>() else {
                return Err(ferr("Vec.insert", "downcast failed"));
            };
            recv.insert(a0, a1)
        };
        task.ram.push_i32(0);
            Ok(Some(()))
        }
        ("Vec", "remove") => {
        let a0: _ = i64::pop_from_stack(task, vm).map_err(|e| ferr("Vec.remove pop", e))? as usize;
        let __r = {
            let handle = task.ram.pop_i32() as u64;
            let Some(obj) = vm.get_heap_object(handle) else {
                return Err(ferr("Vec.remove", "bad handle"));
            };
            let mut guard = obj.write().unwrap();
            let Some(ro) = guard.as_any_mut().downcast_mut::<RustStdlibObject>() else {
                return Err(ferr("Vec.remove", "not RustStdlibObject"));
            };
            let Some(recv) = ro.downcast_mut::<Vec<i64>>() else {
                return Err(ferr("Vec.remove", "downcast failed"));
            };
            recv.remove(a0)
        };
        task.ram.push_i64(__r as i64);
            Ok(Some(()))
        }
        ("Vec", "reverse") => {
        let __r = {
            let handle = task.ram.pop_i32() as u64;
            let Some(obj) = vm.get_heap_object(handle) else {
                return Err(ferr("Vec.reverse", "bad handle"));
            };
            let mut guard = obj.write().unwrap();
            let Some(ro) = guard.as_any_mut().downcast_mut::<RustStdlibObject>() else {
                return Err(ferr("Vec.reverse", "not RustStdlibObject"));
            };
            let Some(recv) = ro.downcast_mut::<Vec<i64>>() else {
                return Err(ferr("Vec.reverse", "downcast failed"));
            };
            recv.reverse()
        };
        task.ram.push_i32(0);
            Ok(Some(()))
        }
        ("Vec", "dedup") => {
        let __r = {
            let handle = task.ram.pop_i32() as u64;
            let Some(obj) = vm.get_heap_object(handle) else {
                return Err(ferr("Vec.dedup", "bad handle"));
            };
            let mut guard = obj.write().unwrap();
            let Some(ro) = guard.as_any_mut().downcast_mut::<RustStdlibObject>() else {
                return Err(ferr("Vec.dedup", "not RustStdlibObject"));
            };
            let Some(recv) = ro.downcast_mut::<Vec<i64>>() else {
                return Err(ferr("Vec.dedup", "downcast failed"));
            };
            recv.dedup()
        };
        task.ram.push_i32(0);
            Ok(Some(()))
        }
        ("Vec", "clone") => {
        let __r = {
            let Some(obj) = vm.get_heap_object(task.ram.pop_i32() as u64) else {
                return Err(ferr("Vec.clone", "bad handle"));
            };
            let guard = obj.read().unwrap();
            let Some(ro) = guard.as_any().downcast_ref::<RustStdlibObject>() else {
                return Err(ferr("Vec.clone", "not RustStdlibObject"));
            };
            let Some(recv) = ro.downcast_ref::<Vec<i64>>() else {
                return Err(ferr("Vec.clone", "downcast failed"));
            };
            recv.clone()
        };
        push_rust_obj(task, vm, "Vec", __r)?;
            Ok(Some(()))
        }
        _ => Ok(None),
    }
}
