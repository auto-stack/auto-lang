# PLAN-592 T3: 016 三轨对拍 oracle 腿——手写 Rust 直调同一 fixture crate。
# 独立工作区(空 [workspace])脱离 auto-lang workspace,路径依赖指回 fixture。
[package]
name = "oracle_016_dep_abi_matrix"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
autolang_abi_matrix = { path = "../fixture/autolang_abi_matrix" }
