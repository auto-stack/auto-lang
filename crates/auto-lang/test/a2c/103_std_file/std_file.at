use auto.io: fopen, fgets, printf, FILE, fclose


fn main() {
    mut fp *FILE = null
    fp = fopen(c"Cargo.toml", c"r")

    mut buf [100]char = [0]
    for fgets(buf, 100, fp) ? {
        printf(c"%s", buf)
    }

    fclose(fp)
}
