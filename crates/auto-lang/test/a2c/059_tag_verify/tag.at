tag Atom {
    Int int
    Char char
    Float float
}

fn main() {
    let atom = Atom.Int(11)

    is atom {
        Atom.Int(i) => print("Got Int:", i)
        Atom.Char(c) => print("Got Char:", c)
        Atom.Float(f) => print("Got Float:", f)
    }
}
