int main(void) {
    bool list = List < int;
    Heap > self->new();
    bool_push(list, 10);
    bool_push(list, 20);
    bool_push(list, 30);

    unknown iter = bool_iter(list);

    unknown a = iter.next();
    unknown b = iter.next();
    unknown c = iter.next();
    unknown d = iter.next();
    return 0;
}
