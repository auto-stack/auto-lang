# Test multi-parameter generic types

enum Pair<K, V> {
    first K
    second V
}

fn main() {
    // Multi-parameter generic
    let p Pair<int, str>
    p.first = 42
    p.second = "hello"
    p
}
