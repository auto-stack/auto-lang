enum Connection {
    Connected int
    Disconnected int
}

fn is_first(m Connection) bool {
    is m {
        Connection.Connected(x) => true
        Connection.Disconnected(x) => false
    }
}

fn main() {
    let s = Connection.Connected()
    is_first(s)
}
