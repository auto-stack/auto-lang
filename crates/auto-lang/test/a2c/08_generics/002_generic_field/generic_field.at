// Test generic type fields in structs (Plan 059 Phase 1)
// Simplified test: just checks that generic type in field declaration works

type Box<T> {
    value T
}

type Container {
    data Box<int>
}

fn main() {
    let x = 42
}
