int main(void) {
    unknown list = List < int;
    Heap > self->new();
    list.push(10);
    list.push(20);
    list.push(30);

    unknown iter = list.iter();

    unknown a = iter.next();
    unknown b = iter.next();
    unknown c = iter.next();
    unknown d = iter.next();
    return 0;
}
