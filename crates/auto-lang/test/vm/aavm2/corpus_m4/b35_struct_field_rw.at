type Counter {
    count int
    step int
}

fn main() {
    let c = Counter { count: 0, step: 3 }
    c.count = c.count + c.step
    c.count = c.count + c.step
    print(c.count)
    c.step = 10
    c.count = c.count + c.step
    print(c.count)
}
