type Point {
    x int
    y int
}

fn mk(x int, y int) Point {
    return Point { x: x, y: y }
}

fn main() {
    let a = mk(3, 4)
    let b = Point { x: a.x * 2, y: a.y * 2 }
    print(a.x + a.y)
    print(b.x - b.y)
}
