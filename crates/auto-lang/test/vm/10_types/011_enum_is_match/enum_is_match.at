enum Color { Red = 1, Green = 2, Blue = 3 }

fn check(c int) str {
    is c {
        1 -> "red"
        2 -> "green"
        3 -> "blue"
        else -> "unknown"
    }
}

print(check(1))
print(check(3))
