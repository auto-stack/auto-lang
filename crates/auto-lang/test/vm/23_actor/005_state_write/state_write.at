task Counter {
    count = 0
    fn start() ! {
    }
    on {
        1 -> {
            count = 42
            print("handler ran")
        }
    }
}

fn main() {
    let h = Task.spawn("Counter", 16)
    h.send(1)
}
