fn main() {
    var result = run_a2r("fn main() {\nvar items List\nitems.push(1)\n}")
    var has_vec = result.find("Vec<i32>")
    if has_vec >= 0 {
        print("ok")
    } else {
        print("fail:" + result)
    }
}
