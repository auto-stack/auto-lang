fn main() {
    var result = run_bytecode("fn main() {\nvar list = List.new()\nlist.push(\"hello\")\nlist.push(\"world\")\nprint(list.get(0))\nprint(list.get(1))\nprint(list.len())\n}")
    if result == "hello\nworld\n2\n" {
        print("ok")
    } else {
        print("fail:" + result)
    }
}
