fn main() {
    var result = run_a2r("enum Option<T> {\nSome(T)\nNone\n}\nfn main() {\nprint(42)\n}")
    var has_enum = result.find("enum Option<T>")
    if has_enum >= 0 {
        print("ok")
    } else {
        print("fail:" + result)
    }
}
