fn main() {
    var result = run_a2r("fn main() {\nvar x List<int>\nprint(x.len())\n}")
    var pos = result.find("Vec<i32>")
    if pos >= 0 {
        print("ok")
    } else {
        print("fail:" + result)
    }
}
