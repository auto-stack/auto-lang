fn main() {
    var result = run_a2r("fn main() {\nvar scores Map<str, int>\n}")
    var has_hashmap = result.find("HashMap<String, i32>")
    if has_hashmap >= 0 {
        print("ok")
    } else {
        print("fail:" + result)
    }
}
