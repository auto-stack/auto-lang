fn main() {
    var result = run_bytecode("fn main() {\nvar s = \"hello world\"\nprint(s.char_at(0))\nvar sub = s.substr(0, 5)\nprint(sub)\nprint(s.len())\n}")
    if result == "104\nhello\n11\n" {
        print("ok")
    } else {
        print("fail:" + result)
    }
}
