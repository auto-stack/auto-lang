fn main() {
    var result = run_bytecode("fn main() {\nvar list = List.new()\nlist.push(\"first\")\nlist.push(\"second\")\nvar v = list.pop()\nprint(v)\nprint(list.len())\n}")
    if result == "second\n1\n" {
        print("ok")
    } else {
        print("fail:" + result)
    }
}
