fn main() {
    var result = run_bytecode("fn main() {\nvar list = List.new()\nlist.push(10)\nlist.push(20)\nlist.push(30)\nprint(list.len())\nprint(list.get(0))\nprint(list.get(2))\n}")
    if result == "3\n10\n30\n" {
        print("ok")
    } else {
        print("fail:" + result)
    }
}
