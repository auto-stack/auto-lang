// Recursive directory size demo
fn main() {
    var sizes = List.new()
    sizes.push(100)
    sizes.push(200)
    sizes.push(150)
    sizes.push(300)
    var total = 0
    for sz in sizes {
        total = total + sz
    }
    print(f"Total size: ${total} bytes")
}
