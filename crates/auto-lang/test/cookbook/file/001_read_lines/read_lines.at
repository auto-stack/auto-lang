// Read lines from file demo
fn main() {
    File.write_text("lines.txt", "Rust\nFun\nAuto")
    let data = File.read_text("lines.txt")
    print(f"Read ${data.len()} chars")
    File.delete("lines.txt")
}
