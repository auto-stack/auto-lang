use.rust std::fs

fn main() ! {
    let entries = fs.read_dir(".").?
    for entry in entries {
        let entry = entry.?
        let name = entry.file_name()
        let name_str = name.to_str().unwrap()
        print(f"Found: $name_str")
    }
}
