use.rust std::fs

fn main() ! {
    let entries = fs.read_dir(".").?
    var count = 0
    for entry in entries {
        let entry = entry.?
        count += 1
    }
    // Verify read_dir returns entries
    assert(count > 0)
}
