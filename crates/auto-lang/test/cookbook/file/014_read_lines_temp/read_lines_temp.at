use.rust std::fs

fn main() {
    let content = "line1\nline2\nline3"
    let path = "test_temp.txt"
    File.write_text(path, content)
    let lines = File.read_text(path)
    for line in lines.lines() {
        print(f"Line: $line")
    }
    File.delete(path)
}
