// Find files by pattern demo
fn main() {
    var files = List.new()
    files.push("src/main.at")
    files.push("src/lib.at")
    files.push("docs/readme.md")
    for f in files {
        if f.ends_with(".at") {
            print(f"Found: ${f}")
        }
    }
}
