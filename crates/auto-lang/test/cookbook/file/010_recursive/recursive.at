// Recursive directory walk demo
fn main() {
    var paths = List.new()
    paths.push("src/main.at")
    paths.push("src/lib.at")
    paths.push("src/sub/deep.at")
    for p in paths {
        var parts = p.split("/")
        print(f"Depth ${parts.len()}: ${p}")
    }
}
