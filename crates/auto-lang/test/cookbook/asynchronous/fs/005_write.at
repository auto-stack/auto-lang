// Write content to file
fn main() ! {
    let data = "Generic data from program!"
    let _ = File.write_text("output.txt", data).?
    let bytes = File.read_bytes("output.txt").?
    print(f"Read ${bytes.len()} bytes")
    let _ = File.delete("output.txt").?
}
