// Read/write demo
fn main() ! {
    let _ = File.write_text("data.txt", "Hello AutoLang!").?
    let bytes = File.read_bytes("data.txt").?
    print(f"Read ${bytes.len()} bytes")
    let _ = File.delete("data.txt").?
}
