// Distribution demo with averaging
fn main() {
    var samples = List.new()
    for i in 0..10 {
        samples.push((i * 3 + 1) % 7)
    }
    var sum = 0
    for v in samples {
        sum = sum + v
    }
    print(f"Average: ${sum / samples.len()}")
}
