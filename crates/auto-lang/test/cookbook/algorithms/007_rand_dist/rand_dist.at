// Normal distribution sample demo
use.rust rand
use.rust rand_distr

fn main() ! {
    var rng = rand.thread_rng()
    var normal = Normal.new(2.0, 3.0).?
    let v f64 = normal.sample(rng)
    print(f"Random from Normal(2, 3): $v")
}
