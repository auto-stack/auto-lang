// Random distribution demo
fn main() {
    var results = List.new()
    results.push(2)
    results.push(4)
    results.push(1)
    results.push(5)
    results.push(3)
    var sum = 0
    for val in results {
        sum = sum + val
    }
    print(f"Sum: ${sum}, Count: ${results.len()}")
}
