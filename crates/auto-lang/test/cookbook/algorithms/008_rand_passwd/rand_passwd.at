// Password generation demo
fn main() {
    var charset = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789"
    var pwd = ""
    for i in 0..16 {
        pwd = pwd + charset.char_at((i * 7 + 3) % charset.len())
    }
    print(f"Password: ${pwd}")
}
