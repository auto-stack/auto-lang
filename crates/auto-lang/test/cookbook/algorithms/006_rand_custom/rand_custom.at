// Custom random value demo
enum Pet {
    Dog int
    Cat int
}

fn main() {
    let pet = Pet.Dog(3)
    is pet {
        Pet.Dog(n) -> print(f"Dog: ${n}"),
        Pet.Cat(n) -> print(f"Cat: ${n}")
    }
}
