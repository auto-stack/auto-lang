// Random choice demo
fn main() {
    var choices = List.new()
    choices.push("apple")
    choices.push("banana")
    choices.push("cherry")
    let idx = 42 % choices.len()
    print(f"Selected: ${choices.get(idx)}")
}
