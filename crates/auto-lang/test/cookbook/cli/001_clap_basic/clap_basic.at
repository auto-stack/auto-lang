// CLI argument parsing demo
type Args {
    pattern str
    path str
}

ext Args {
    static fn parse() Args {
        return Args("hello", "input.txt")
    }
}

fn main() {
    let args = Args.parse()
    print(f"Pattern: ${args.pattern}, Path: ${args.path}")
}
