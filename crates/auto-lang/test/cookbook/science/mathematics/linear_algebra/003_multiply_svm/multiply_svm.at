fn main() {
    let scalar = 2
    let v = [1, 2, 3]
    var result = [0, 0, 0]
    for i in 0..3 {
        result[i] = scalar * v[i]
    }
    assert(result[0] == 2)
    assert(result[1] == 4)
    assert(result[2] == 6)
}
