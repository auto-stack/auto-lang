// Complex number creation demo
type Complex {
    real f64
    imag f64
}

fn main() {
    let c = Complex(3.0, 4.0)
    let norm = c.real * c.real + c.imag * c.imag
    print(f"Norm squared: ${norm}")
}
