// Auto-generated by a2r transpiler

// a2r Standard Library (from crate)
#[allow(unused_imports)]
use auto_lang::a2r_std;
use auto_lang::a2r_std::*;

use log::info;
use chrono::Local;
use env_logger::Builder;
use std::io::Write;
fn main() {
    Builder::new().format(|buf, record| { writeln!(buf, "[{}] {} - {}", chrono::Local::now().format("%Y-%m-%d %H:%M:%S"), record.level(), record.args()) }).init();
    info!("application started");
    info!("with timestamp logging");
}
