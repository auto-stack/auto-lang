use.rust std::process::Command
use.rust std::string::String

fn main() ! {
    let output = Command.new("ls").args(["-la"]).output().?
    print(f"Exit code: ${output.status}")
    print(f"Stdout: ${String.from_utf8(output.stdout).?}")
}
