// Redirect output to file demo
fn main() ! {
    let _ = File.write_text("output.log", "Processing complete").?
    let data = File.read_bytes("output.log").?
    print(f"Wrote ${data.len()} bytes to log")
    let _ = File.delete("output.log").?
}
