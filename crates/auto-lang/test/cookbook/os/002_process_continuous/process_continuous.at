// Process output capture
fn main() {
    print("Starting...")
    let result = Command.new("echo").arg("hello").output()
    print("Processing...")
    let stdout = result.stdout
    print("Done")
}
