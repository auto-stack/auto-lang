dep chrono
use.rust chrono::Utc
use.rust chrono::TimeZone

fn main() {
    let dt = Utc.timestamp_opt(1700000000, 0).single().unwrap()
    print(f"From timestamp: $dt")
    let now = Utc.now()
    let ts = now.timestamp()
    print(f"Current timestamp: $ts")
}
