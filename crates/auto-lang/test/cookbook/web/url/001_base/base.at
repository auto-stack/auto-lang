use.rust url::Url

fn main() {
    let parsed = Url.parse("https://example.com/path?query=1").unwrap()
    print(f"Scheme: ${parsed.scheme()}")
    print(f"Host: ${parsed.host_str()}")
    print(f"Path: ${parsed.path()}")
}
