fn main() {
    let url = "https://example.com/path?query=1"
    let encoded = Url.encode("hello world")
    let decoded = Url.decode("hello%20world")
    print(f"Scheme: ${Url.scheme(url)}")
    print(f"Encoded: $encoded")
    print(f"Decoded: $decoded")
}
