// Paginated API request demo (simulated)

type Dependency {
    id str
}

fn main() {
    var deps List<Dependency> = List.new()
    deps.push(Dependency("serde_derive"))
    deps.push(Dependency("serde_json"))

    for d in deps {
        print(f"Depends on serde: ${d.id}")
    }
}
