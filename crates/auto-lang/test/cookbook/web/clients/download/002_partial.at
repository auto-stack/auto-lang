// Partial/range download demo (simulated)

fn main() {
    let total = 10000
    let chunk_size = 1024
    var downloaded = 0

    for i in 0..10 {
        downloaded = downloaded + chunk_size
        if downloaded > total {
            downloaded = total
        }
        print(f"Downloaded ${downloaded}/${total}")
    }
}
