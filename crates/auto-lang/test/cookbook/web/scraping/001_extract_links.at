// HTML link extraction demo (simulated)

fn main() {
    var links List<str> = List.new()
    links.push("/page1")
    links.push("/page2")

    for href in links {
        print(f"Link: ${href}")
    }
}
