// Parallel search demo
fn main() {
    var data = List.new()
    data.push(1); data.push(5); data.push(10); data.push(2)
    var target = 10
    var found = false
    for val in data { if val == target { found = true } }
    print(f"Found ${target}: ${found}")
}
