// Thread pool directory walk demo
fn main() {
    var dirs = List.new()
    dirs.push("src/")
    dirs.push("docs/")
    dirs.push("test/")
    let count = dirs.len()
    for d in dirs {
        print(f"Walking: ${d}")
    }
    print(f"Walked ${count} directories")
    assert(true)
}
