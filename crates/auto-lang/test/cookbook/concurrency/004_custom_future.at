dep tokio
use.rs std::time::Duration
use.rs std::time::Instant

fn main() ~void {
    let start = Instant.now()
    print("waiting 10 ms...")
    let elapsed = start.elapsed()
    print(f"done in ${elapsed}")
    assert(true)
}
