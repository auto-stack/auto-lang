fn main() {
    let messages = ["hello", "world"]
    var msg_count = 0
    for msg in messages {
        msg_count += 1
        if msg == "world" {
            break
        }
    }
    assert(msg_count == 2)
}
