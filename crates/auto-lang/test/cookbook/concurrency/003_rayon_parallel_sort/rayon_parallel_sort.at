// Parallel sort demo
fn main() {
    var data = List.new()
    data.push(5)
    data.push(3)
    data.push(1)
    data.push(4)
    data.push(2)
    data.sort()
    for val in data { print(f"  ${val}") }
}
