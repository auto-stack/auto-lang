fn parse_parts(s str) {
    var parts = s.split(",")
    let first = parts.next().unwrap()
    let second = parts.next().unwrap()
    print(f"First: ${first}")
    print(f"Second: ${second}")
}

fn main() {
    parse_parts("hello,world")
}
