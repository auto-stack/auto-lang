use.rust regex::Regex

fn main() {
    let re = Regex.new(r"#\w+").unwrap()
    let text = "Love #rust and #programming!"
    for cap in re.captures_iter(text) {
        print(f"Hashtag: ${cap.get(0)}")
    }
}
