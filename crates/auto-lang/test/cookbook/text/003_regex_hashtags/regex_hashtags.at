// Hashtag extraction demo
fn main() {
    let tweet = "Learning #AutoLang and #RustLang today!"
    var words = tweet.split(" ")
    for w in words {
        if w.starts_with("#") { print(f"Hashtag: ${w}") }
    }
}
