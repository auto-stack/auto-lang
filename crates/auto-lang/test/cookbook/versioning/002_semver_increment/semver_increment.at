// Semver increment demo
type SemVer {
    major int
    minor int
    patch int
}

fn main() {
    var v = SemVer(1, 2, 3)
    print(f"${v.major}.${v.minor}.${v.patch}")
    v.patch = v.patch + 1
    print(f"${v.major}.${v.minor}.${v.patch}")
}
