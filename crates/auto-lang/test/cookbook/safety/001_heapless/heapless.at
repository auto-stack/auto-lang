dep heapless
use.rust heapless::Vec

fn main() {
    // heapless::Vec — fixed-capacity stack buffer, no heap allocation
    var buf = Vec.new()
    let data = "hello".as_bytes()
    for byte in data {
        buf.push(byte).unwrap()
    }
    print(f"Buffer: ${buf.len()} bytes")
}
