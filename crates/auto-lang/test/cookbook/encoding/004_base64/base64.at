// Simple encoding demo
fn main() {
    let text = "hello"
    print(f"Original: ${text}")
    let upper = text.to_uppercase()
    print(f"Uppercase: ${upper}")
    let lower = upper.to_lowercase()
    print(f"Lowercase: ${lower}")
}
