fn main() {
    let value = 0x1234
    let bytes = value.to_be_bytes()
    print(f"BE bytes: ${bytes[0]}, ${bytes[1]}")
    let le_bytes = value.to_le_bytes()
    print(f"LE bytes: ${le_bytes[0]}, ${le_bytes[1]}")
    let from_be = u16.from_be_bytes(bytes)
    print(f"From BE: ${from_be}")
}
