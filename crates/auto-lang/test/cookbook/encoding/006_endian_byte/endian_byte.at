fn main() {
    let value = 0x1234
    let bytes = value.to_be_bytes()
    print(f"BE bytes: ${bytes[0]}, ${bytes[1]}, ${bytes[2]}, ${bytes[3]}")
    let le_bytes = value.to_le_bytes()
    print(f"LE bytes: ${le_bytes[0]}, ${le_bytes[1]}, ${le_bytes[2]}, ${le_bytes[3]}")
}
