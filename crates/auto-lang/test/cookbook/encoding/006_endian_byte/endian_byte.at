fn main() {
    let value = 0x1234
    let bytes = value.to_be_bytes()
    assert(bytes[0] == 0)
    assert(bytes[2] == 0x12)
    assert(bytes[3] == 0x34)
    let le_bytes = value.to_le_bytes()
    assert(le_bytes[0] == 0x34)
    assert(le_bytes[1] == 0x12)
}
