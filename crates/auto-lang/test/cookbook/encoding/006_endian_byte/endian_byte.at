// Endian byte operations demo
fn main() {
    let val = 0x1234
    let hi = val / 256
    let lo = val % 256
    print(f"Value: ${val}")
    print(f"Big-endian bytes: [${hi}, ${lo}]")
    print(f"Little-endian bytes: [${lo}, ${hi}]")
}
