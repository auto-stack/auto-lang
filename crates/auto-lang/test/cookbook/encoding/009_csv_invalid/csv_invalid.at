fn main() {
    let data = "name,age\nAlice,30\nBob,twenty-five\nCharlie,35"
    var lines = data.split("\n")
    assert(lines.len() == 4)
    // Simplified: test that we can detect "twenty-five" as non-numeric
    assert(data.contains("twenty-five"))
}
