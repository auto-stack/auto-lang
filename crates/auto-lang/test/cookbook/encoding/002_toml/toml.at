dep serde
dep toml
use.rust serde::Deserialize
use.rust toml

type Config {
    title str
    owner Owner
}

type Owner {
    name str
}

fn main() {
    let toml_str = "[owner]\nname = \"Alice\""
    let config Config = toml.from_str(toml_str).unwrap()
    print(f"Owner: ${config.owner.name}")
}
