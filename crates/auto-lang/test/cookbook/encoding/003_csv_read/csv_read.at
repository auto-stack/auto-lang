dep csv
use.rust csv::Reader

fn main() {
    let csv_str = "name,age\nAlice,30\nBob,25"
    var reader = Reader.from_reader(csv_str.as_bytes())
    var count = 0
    for result in reader.records() {
        let record = result.?
        count += 1
    }
    assert(count == 2)
}
