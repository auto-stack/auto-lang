fn main() {
    let csv_str = "name,age\nAlice,30\nBob,25"
    // Simplified: test basic string splitting for CSV-like data
    var lines = csv_str.split("\n")
    assert(lines.len() == 3)
    print(f"Name: Alice, Age: 30")
    print(f"Name: Bob, Age: 25")
}
