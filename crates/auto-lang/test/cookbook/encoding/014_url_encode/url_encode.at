// URL encoding demo — encodes special characters
fn main() {
    let input = "hello world! foo=bar&baz=qux"
    let encoded = input.replace(" ", "+").replace("!", "%21").replace("&", "%26").replace("=", "%3D")
    print(f"Encoded: ${encoded}")
}
