// CSV serialization demo
fn main() {
    var writer = List.new()
    writer.push("name,age")
    writer.push("Alice,30")
    writer.push("Bob,25")
    print(f"Header: ${writer.get(0)}")
    for i in 1..writer.len() {
        print(writer.get(i))
    }
}
