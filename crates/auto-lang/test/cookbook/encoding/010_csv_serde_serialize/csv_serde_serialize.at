// CSV serialization demo
fn main() {
    var rows = List.new()
    rows.push("name,age,decade")
    rows.push("Alice,30,30s")
    rows.push("Bob,25,20s")
    for i in 1..rows.len() {
        print(rows.get(i))
    }
}
