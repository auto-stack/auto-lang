dep percent_encoding
use.rust percent_encoding

fn main() {
    let input = "hello world! foo@bar.com"
    let encoded = percent_encoding.percent_encode(input.as_bytes(), percent_encoding.NON_ALPHANUMERIC)
    print(f"Encoded: $encoded")
}
