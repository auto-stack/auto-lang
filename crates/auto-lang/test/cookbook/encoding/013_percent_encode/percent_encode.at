dep percent_encoding
use.rust percent_encoding::percent_encode
use.rust percent_encoding

fn main() {
    let input = "hello world! foo@bar.com"
    let encoded = percent_encode(input.as_bytes(), percent_encoding.NON_ALPHANUMERIC).to_string()
    assert(encoded.contains("hello"))
}
