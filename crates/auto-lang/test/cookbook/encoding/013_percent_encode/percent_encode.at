// URL percent-encoding demo
fn main() {
    let input = "hello world! foo@bar.com"
    let encoded = input.replace(" ", "%20").replace("!", "%21").replace("@", "%40")
    print(f"Encoded: ${encoded}")
}
