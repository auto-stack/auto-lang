use.rs std::option::Option
use.rs std::result::Result

fn main() {
    let a Option<int> = Some(42)
    let b Option<int> = None
    let c Result<int, str> = Ok(10)
    let d Result<int, str> = Err("fail")
}
