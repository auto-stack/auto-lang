// Auto-generated by a2r transpiler

#[derive(Clone, Debug, PartialEq, Eq, PartialOrd, Ord)]
struct Meta {
    pub key: String,
    pub tag: String,
}

#[derive(Clone, Debug, PartialEq, Eq, PartialOrd, Ord)]
struct Node {
    pub name: String,
    pub meta: Meta,
}

fn takes(s: &str) {
    println!("{}", s);
}

fn getMeta() -> Meta {
    return Meta { key: "mk".to_string(), tag: "mt".to_string() };
}

fn getIdx(xs: Vec<String>, i: i64) -> String {
    return xs[(i) as usize].clone();
}

fn useAll(n: Node, mut names: Vec<String>, i: i64) {

    takes(n.name.as_str());

    takes(n.meta.key.as_str());
    takes(n.meta.tag.as_str());


    takes(getIdx(names.clone(), i).as_str());

    takes(getMeta().key.as_str());

    let mut t: String = "".to_string();
    for s in &names {
        t = format!("{}{}", t, s);
    }
    takes(t.as_str());

    println!("{}", (n.name.len() as i64));
}

fn main() {
    let n = Node { name: "nn".to_string(), meta: Meta { key: "nk".to_string(), tag: "nt".to_string() } };
    let mut names: Vec<String> = vec!["a".to_string(), "b".to_string()];
    useAll(n.clone(), names.clone(), 1);
}
