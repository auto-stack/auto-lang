// Plan 380 G1: a2r 编译期 #{expr} 求值 — read_text 文件嵌入 + 字面量

// 测试1：#{read_text("...")} 把文件内容嵌入为字符串字面量（include_str! 等价）
fn banner() str {
    return #{read_text("test/a2r/16_interop/017_comptime_read_text/greeting.txt")}
}

// 测试2：整数字面量 fallback
fn answer() int {
    return #{42}
}

// 测试3：字符串字面量 fallback
fn tag() str {
    return #{"comptime"}
}

// 测试4：comptime 嵌入作 const 初始化器——类型必须推断为 &str
//（Plan 032 G1：曾推断 Unknown，发射 `const X: /* unknown */` 非法 Rust，
//  消费端 auto-ai 靠 sed 兜底改 &str）
const SOUL = #{read_text("test/a2r/16_interop/017_comptime_read_text/greeting.txt")}
