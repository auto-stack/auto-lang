task Router {
    fn start() ! {
    }
    on {
        1 -> {
            print("matched one")
        }
        else -> {
            print("fell through")
        }
    }
}

fn main() {
    let h = Task.spawn("Router", 16)
    h.send(99)
}
