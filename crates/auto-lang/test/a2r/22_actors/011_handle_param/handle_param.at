task Counter {
    count = 0
    fn start() ! {
    }
    on {
        1 -> {
            count = count + 1
            print("got one")
        }
    }
}

fn forward(h TaskRef<i64>) {
    h.send(1)
    h.send(1)
}

fn main() {
    let h = Task.spawn("Counter", 16)
    forward(h)
}
