// Auto-generated by a2r transpiler

// a2r Standard Library (from crate)
#[allow(unused_imports)]
use a2r_std;
use a2r_std::*;

#[derive(Clone, Debug)]
struct Echo {
}

impl Echo {
    pub fn new() -> Self {
        Self {}
    }

    pub async fn start(&mut self) -> Result<(), Box<dyn std::error::Error>> {
        Ok(())
    }

    pub async fn stop(&mut self) -> Result<(), Box<dyn std::error::Error>> {
        Ok(())
    }

    pub async fn handle_msg(&mut self, msg: i64, reply_tx: a2r_std::task::NopReply) -> Result<(), Box<dyn std::error::Error>> {
        match msg {
            1i64 => {
                println!("got one");
            }
            2i64 => {
                println!("got two");
            }
            _ => {
                let _ = msg;
            }
        };
        Ok(())
    }
}

pub fn spawn_echo(__rt: &mut a2r_std::task::ActorRuntime) -> a2r_std::task::TaskRef<i64> {
    let (tx, mut rx) = tokio::sync::mpsc::unbounded_channel::<i64>();
    let join = tokio::spawn(async move {
        let mut actor = Echo::new();
        let _ = actor.start().await;
        while let Some(msg) = rx.recv().await {
            let reply_tx = a2r_std::task::NopReply;
            let _ = actor.handle_msg(msg, reply_tx).await;
        }
    });
    __rt.register(a2r_std::task::TaskHandle::new(tx, join))
}


#[tokio::main]
async fn main() {
    let mut __rt = a2r_std::task::ActorRuntime::new();
    let h = spawn_echo(&mut __rt);
    h.send(1);
    h.send(2);
    h.send(1);
    drop(h);
    __rt.run_to_completion().await;
}
