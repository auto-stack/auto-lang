// Auto-generated by a2r transpiler

/// Plan 610 ⑤: C ABI export face for `dup` — the sibling wrapper bridges C widths
/// to the safe body below; the exported symbol equals the fn name (no_mangle
/// ignores the module path).
mod dup_c_export {
    #[unsafe(no_mangle)]
    pub extern "C" fn dup(s: *const std::os::raw::c_char) -> *mut std::os::raw::c_char {
        let s_conv: String = if s.is_null() { String::new() } else { unsafe { std::ffi::CStr::from_ptr(s) }.to_string_lossy().into_owned() };
        std::ffi::CString::new(super::dup(&s_conv)).unwrap_or_default().into_raw()
    }
}
fn dup(s: &str) -> String {
    return format!("{}{}", s, s);
}

/// Plan 610 ⑤: C ABI export face for `greet` — the sibling wrapper bridges C widths
/// to the safe body below; the exported symbol equals the fn name (no_mangle
/// ignores the module path).
mod greet_c_export {
    #[unsafe(no_mangle)]
    pub extern "C" fn greet(name: *const std::os::raw::c_char, times: i32) -> *mut std::os::raw::c_char {
        let name_conv: String = if name.is_null() { String::new() } else { unsafe { std::ffi::CStr::from_ptr(name) }.to_string_lossy().into_owned() };
        std::ffi::CString::new(super::greet(&name_conv, times as i64)).unwrap_or_default().into_raw()
    }
}
fn greet(name: &str, times: i64) -> String {
    let mut out: String = "".to_string();
    for i in 0..times {
        out = format!("{}{}", out, name);
    }
    return out;
}

fn main() {
    println!("{}", dup("ab"));
    println!("{}", greet("hi", 3));
}
