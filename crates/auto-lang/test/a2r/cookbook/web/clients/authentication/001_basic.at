// HTTP Basic auth demo (simulated)

fn main() {
    let user = "user"
    let pass = "pass"
    print(f"Authenticating as ${user}:${pass}")
    let body = "authenticated response"
    print(f"Body: ${body}")
}
