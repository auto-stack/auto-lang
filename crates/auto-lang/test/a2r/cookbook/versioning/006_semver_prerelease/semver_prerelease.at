use.rust semver::Version

fn main() {
    let v = Version.parse("1.0.0-alpha.1").unwrap()
    print(f"Major: ${v.major}")
    print(f"Pre: ${v.pre}")
    let is_pre = !v.pre.is_empty()
    print(f"Is prerelease: $is_pre")
}
