// Async file reading demo (simulated)

fn process_data(data str) {
    print(f"Data Length: ${data.len()}")
}

fn main() {
    let content = "Hello from data.txt"
    process_data(content)

    let str_content = "More text data"
    print(f"Text: ${str_content}")
}
