// Test move (ownership transfer) expression
// Plan 122: .move replaces deprecated .take
fn main() {
    let s1 = str_new("hello", 10)
    let s2 = s1.move
    print(str_len(s2))
    // s1 is no longer valid here (would be compiler error in full implementation)
}
