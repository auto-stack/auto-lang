// Test view (immutable borrow) expression
fn main() {
    let s String = String.from("hello")
    let slice = s.view
    print(slice.len)  // slice has a `len` property
    print(s)  // s is still valid after use of slice
}
