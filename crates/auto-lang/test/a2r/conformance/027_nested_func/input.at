// Conformance 027: Nested function calls
fn double(x int) int {
    return x * 2
}

fn add_one(x int) int {
    return x + 1
}

fn main() {
    print(double(add_one(4)))
    print(add_one(double(3)))
}
