fn main() {
    let list = List.new()

    // Test basic methods
    let len = list.len()
    let is_empty = list.is_empty()

    // Test modification methods
    list.push(10)
    list.push(20)
    list.push(30)

    let new_len = list.len()
}
