// composables.ts — Plan 442 A3 corpus: TS composable imported directly by
// the root use.web clause (no .at adapter). `useT` becomes a stub.
export function useT() {
  return (key: string) => key
}
