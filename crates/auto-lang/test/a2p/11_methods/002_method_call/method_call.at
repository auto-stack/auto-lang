type Counter {
    count int

    fn inc() {
        .count = .count + 1
    }

    fn get() int {
        .count
    }
}

fn main() {
    let c = Counter(count: 0)
    c.inc()
    c.inc()
    print(c.get())
}
