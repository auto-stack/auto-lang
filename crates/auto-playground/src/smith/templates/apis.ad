# APIs

## Authentication

All endpoints require a session cookie or API key header:
```
Authorization: Bearer <api_key>
```

## Endpoints

### Sessions

| Method | Path | Description |
|--------|------|-------------|
| GET | `/api/smith/forge/session` | List all sessions |
| POST | `/api/smith/forge/session` | Create new session |
| GET | `/api/smith/forge/session/{sid}` | Get session details |
| PATCH | `/api/smith/forge/session/{sid}` | Rename session |
| DELETE | `/api/smith/forge/session/{sid}` | Delete session |
| POST | `/api/smith/forge/session/{sid}/chat` | Send chat message |
| GET | `/api/smith/forge/session/{sid}/chat/stream` | Stream chat response |

### Ledger

| Method | Path | Description |
|--------|------|-------------|
| GET | `/api/smith/forge/ledger/{project}` | Get ledger document |
| POST | `/api/smith/forge/ledger/{project}/section/{section}` | Update section |
| POST | `/api/smith/forge/ledger/{project}` | Update full ledger |

## Schemas

### ForgeSession

```json
{
  "id": "uuid-string",
  "name": "optional-session-name",
  "project_path": "/path/to/project",
  "status": "Idle | Active | Streaming",
  "phase": "Discovery | Spec | Code | Verify",
  "messages": [...],
  "created_at": 1715500000,
  "phase_history": [...]
}
```

### LedgerDocument

```json
{
  "project": "project-name",
  "version": 1,
  "sections": [
    {
      "id": "goals",
      "section_type": "goals",
      "title": "🎯 Goals",
      "status": "draft",
      "content": "# markdown content...",
      "depends_on": [],
      "last_modified": 1715500000,
      "last_verified": null
    }
  ]
}
```
