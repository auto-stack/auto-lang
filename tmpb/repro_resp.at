use.rust axum::response::Response
use.rust axum::Json
use.rust serde::Serialize

#[derive(Debug, Serialize)]
pub type OkBody { token str }

fn login(ok bool) ~Response {
    if ok {
        return ok_response(OkBody(token: "abc"))
    }
    return err_response("invalid credentials")
}
