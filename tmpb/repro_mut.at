pub type Item {
    status str
}

pub type Doc {
    items List<Item>
}

fn set_status(doc &mut Doc, idx int, s str) void {
    doc.items[idx].status = s
}

fn set_first(doc Doc) Doc {
    let mut d = doc
    d.items[0].status = "x"
    return d
}
