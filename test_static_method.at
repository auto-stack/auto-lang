type TestType {
    data List

    fn create() TestType {
        TestType(data: List.new())
    }

    fn len() int {
        let data = .data
        data.len()
    }
}

fn main() {
    let t = TestType.create()
    t.len()
}
