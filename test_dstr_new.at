type dstr {
    data List

    fn create() dstr {
        dstr(data: List.new())
    }

    fn len() int {
        let data = .data
        data.len()
    }
}

fn main() {
    let s = dstr.create()
    s.len()
}
