// Test pointer types and operations
type Point {
    x int
    y int
}

fn main() {
    let p = Point { x: 10, y: 20 }

    // Test 1: Pointer type declaration
    let ptr *int = nil

    // Test 2: Address-of operator
    // let addr = &p.x

    // Test 3: Dereference operator
    // let value = *ptr

    print("Done")
}
