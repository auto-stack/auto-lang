[package]
name = "tokio-stream-tests"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "=1.0", features = ["rt", "macros"] }
async-stream = "0.3"
futures = "0.3"

# Keep this oracle crate out of the parity workspace.
[workspace]
