
type Heap<T> {
    ptr *T
    cap u32
}

ext Heap<T> {
    #[c, vm, pub]
    static fn new() Heap<T>

    #[c, pub]
    fn data() *T

    #[c, pub]
    fn capacity() u32

    #[c, vm, pub]
    fn try_grow(min_cap u32) bool

    #[c, vm, pub]
    fn drop()
}
