# Generated by Cargo
# will have compiled files and executables
debug/
target/

# Remove Cargo.lock from gitignore if creating an executable, leave it for libraries
# More information here https://doc.rust-lang.org/cargo/guide/cargo-toml-vs-cargo-lock.html
Cargo.lock

# These are backup files generated by rustfmt
**/*.rs.bk


# Added by cargo

/target

# hisotry
.history.txt
history.txt

# Test related

*.wrong.c
*.wrong.h
*.wrong.rs
*.wrong.ts
*.wrong.vue
*.wrong.out
# cookbook_vm_tests 失配副产物(P499-7 清偿:误提交的 60 个已删)
wrong.out
*.wrong.result
*.wrong.ets
*.wrong.gd
*.wrong.tscn
*.wrong.js

# IAR IDE
.arts/

.am/
build/

*.log

*.o
*.exe

# Temporary test files (root-anchored: the un-anchored `test*.rs` silently
# skipped crates/auto-lang/src/vm/tests_string_pool.rs at `git add`, leaving
# master with a mod declaration but no file (Plan 417-E3 merge incident);
# subdir test files are all tracked, so anchoring surfaces no new noise)
tmp/
/test*.at
/test*.c
/test*.h
/test*.rs
/test*.sh
/test*.pdb
# Temp files with encoding issues
tmp/*.at

examples/**/dist/*
examples/**/gen/
examples/**/.auto/
examples/**/ark/
examples/**/jet/
examples/**/vue/
examples/**/deps/
deps/

# Vue project output at root
/dist/
/vue/

# npm
node_modules/
# Local-only, not committed
crates/xdm-gen/
xdm-gen/

# Auto self-hosted compiler generated output
auto/rust/

# Git worktrees
.worktrees/

# Local IDE/agent settings
.claude/
.agent/
.zcode/

# Graphify knowledge graph output (regenerate with /graphify)
graphify-out/
graphify-out-crates/

# a2r transpiler output
*.a2r.rs

# Backup files
*.bak

# TypeScript build info
*.tsbuildinfo

# Temp config/test files
config_test.txt
test-deployed.js

# Local dev tools
.mcp.json
.playwright-mcp/
.playwright-cli/
bun.lock

# Auto-generated backend skeleton
examples/**/rust/

# Auto-generated rust-workspace output (a2r transpiled main.rs, etc.)
# These are build artifacts regenerated from .at sources — never track them.
examples/**/rust-workspace/

# AutoUI test screenshots & artifacts (local only, do not track binary images in git)
examples/ui/**/tests/screenshots/
examples/ui/**/src/front/tests/screenshots/
tests/screenshots/
tests/playwright-report/
tests/test-results/
helloworld_test.png

# Cookbook test run artifacts
data.txt
lines.txt
out.txt
test_temp.txt


# Windows null-device artifact (git-bash 2>/dev/null creates a literal file)
/nul

# Windows MSVC debug symbols (program database)
*.pdb

# Local migration backups (pre-spec-migration snapshots)
/.backup/
*.json
# I6 双端共享布局期望值表（465 T4 产出；472 T2 修复入库）
!crates/auto-lang/src/ui/layout_cases.json
# Plan 489：测试 corpus 的 i18n 平铺表（*.json 母规则曾吞掉 zh.json——
# plan050_stub_nil 落测时未入库，fresh clone i18n 双测必红，P487-2 债）
!crates/auto-lang/test/**/i18n/*.json
# Plan 516：wm 测试位与渲染器包的 package.json（*.json 母规则曾吞掉
# 508 的 packages/drawlist-renderer/package.json——fresh clone 测试位缺件）
!crates/auto-man/wm-test/package.json
!packages/drawlist-renderer/package.json
!examples/remote/viewer/package.json
!examples/ui/**/package.json
playwright-report/
test-results/
__pycache__/
*.pyc
.worktree/

# Local junctions for cross-repo path deps (worktree-only, at repo root)
# Root-anchored: unanchored `auto-lang/` would also match crates/auto-lang/
# and silently drop new tracked test fixtures under it.
/auto-ai/
/auto-lang/
node_modules
tmpb/
target-412/
examples/widgets-gallery/src/front/tests/

# auto CLI writes a local am.at cache into the run directory; keep generated copies out of the tree (assets copy is tracked explicitly)
examples/**/am.at

# auto-plan 四技能为 auto-musk 仓所有，本仓以 junction 引用（机器本地安装物，禁入库；源=../auto-musk/.agents/skills）
.agents/skills/auto-plan-new/
.agents/skills/auto-plan-work/
.agents/skills/auto-plan-review/
.agents/skills/auto-plan-merge/

# cargo 探针构建目录（worktree-less probe builds，可再生；先例 target-412/）
target-*/
# scratch=工作证据区（不入库；有价值者经 docs/reports|scripts/repro|test 升格，见 2026-09-07 三分归位）
scratch/
