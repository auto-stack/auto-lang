fn main() {
    let map = HashMap.new()
    map.insert_str("name", "Alice")
    let has_name = map.contains("name")
    let has_age = map.contains("age")
    map.drop()

    if has_name == 1 {
        "has_name is 1"
    } else {
        "has_name is not 1"
    }
}
