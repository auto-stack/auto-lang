# Plan 466: full 档 nextest 配置(cargo tf / cargo ta 经 --config-file 指定)。
# --config-file 是整体替换而非合并。review / fold 前的全量门禁专用。
# 本文件需自成一体。
# Plan 466: 不设 str_churn 过滤 → 1M 大档照跑。
# Plan 532: 嵌套塔里程碑测试(aavm2_t3_tower_milestone,小时级)排除
# ——仅 cargo t3(大版本升级档,nextest-t3.toml)运行。
[profile.default]
default-filter = "not test(aavm2_t3_tower_milestone)"
